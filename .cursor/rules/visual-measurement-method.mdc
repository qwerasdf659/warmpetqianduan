---
description: 视觉对标时「怎么量」的方法论——指标选错/量法失效导致的连环误判，以及每类问题该用哪个判据
alwaysApply: true
---

# 量法本身会骗人

规则 `silent-failure-and-verification` 管的是「**量错了对象**」（量了中间产物、
量了旧缓存）。本规则管的是另一类：**对象量对了，但指标选错或量法失效**，
于是拿着一个涨了的数字往错的方向连推几轮。

这一整类问题在 2026-09-16 那轮招牌对标里集中爆发：同一个问题量错四次、
三次结论互相矛盾，全部是量法问题而不是眼力问题。

## 铁律：指标涨了不等于画面对了

改完必须**回头看图**，哪怕数字很漂亮。

实测：文字描边宽度从 2 加到 3，「过渡像素占比」从 29.2% 涨到 56.4%
（竞品 65.0%），看着像大幅改好。实际是**相邻笔画的描边糊成一片**，
把字之间的底色填满，用户看到「一坨棕色」。
涨的是糊掉的棕色 —— 而糊掉的棕色本身就落在「过渡像素」这个区间里，
**那个指标对这种失败完全无感**。

## 每类问题用哪个判据

| 要判断的事 | 用什么 | 不能用什么 |
| --- | --- | --- |
| 小字的**字色 vs 描边色** | `pixel-grid.mjs` 打明度网格，肉眼找横画 | 直方图、包围盒（分不清笔画和它的描边） |
| 某个元素**该不该存在** | 该行**主色占比**（≈50% = 实心带；个位数 = 淡过渡） | 对比色值（只能告诉你深浅） |
| 谁是**底色**、谁是**前景** | 明度直方图，**面积最大的一档是底** | 单点取色（会取到某个像素） |
| 画面「简陋」 | `dark-mass.mjs` 看明度分布是否全堆在最亮档 | `edge-strength` 的「最深像素」（只说明存在暗像素） |
| 亮度 | 感知加权 Rec.601 | `max(r,g,b)`（高饱和暖色会偏高，粉色 `#E08C9E` max=224 但感知只有 167） |

## 采样坐标不能靠肉眼猜

四次量错竞品字色，每次都是先猜坐标再解释数据。**猜错坐标时数据自洽但结论错**。

正确做法：**先自动定位，再量**。

```bash
# 找暗像素包围盒 → 定位文字实际位置
node scripts/find-text-rows.mjs <png> <x0> <x1> [y0] [y1] [阈值]
# 找颜色方差最小的连续横带 → 定位留字位
node scripts/find-band.mjs <png>
```

判断采样是否落空：**量出「100% 单色」或「全 0」时不是结论，是采样区域不对。**

## 改配色之前先问「该不该有」

墙地交界的踢脚板改了两轮都没解决，两轮都在调颜色（深棕 → 浅一档的棕）。
第三次才发现**竞品那里根本没有踢脚板**。

同类：招牌的白色背景 —— 我为了「方便程序写字」在提示词里要求
「牌面必须是纯净的平涂奶白色」，得到一块 89% 近白的贴片。
**程序需要的是「平整」，不是「白」。**

提约束时问一句：**这是画面需要的，还是我为了省事需要的？**

## 认知写在注释里 ≠ 实现跟上了

踢脚板那段代码的注释当时就写对了（「参考图的墙地交界几乎看不出线，
视线是滑过去的」），但画出来的是 L=198 的实心带、和墙纸差 35 级明度。
**这种脱节靠读代码发现不了，只能靠量。**

## 限制点会随参数变

`placeArt` 的 `scale = min(框宽/图宽, 框高/图高)`，哪个在起作用**会变**。
「加宽没用要加高」这条结论在取消缩小系数后就反了（变成受宽度限制，
加高完全不起作用）。**每次调完重算 `min`，别照上一轮的结论推理。**

## 脚本清单

全部在 skill `ui-visual-parity` 的 `scripts/`，首次用先拷进 `.build/`：

```powershell
Copy-Item .cursor\skills\ui-visual-parity\scripts\*.mjs .build\scripts\ -Force
```

依赖装在 `.build/node_modules`，ESM 按脚本自身位置往上找 —— 必须在 `.build/` 下跑。
