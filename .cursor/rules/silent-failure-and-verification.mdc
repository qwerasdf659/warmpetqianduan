---
description: 如何确认改动真的生效，以及识别退出码 0 的静默失败
alwaysApply: true
---

# 静默失败与「改动到底生效了没」

本项目最贵的返工不是写错代码，而是**验证错了对象** —— 量了中间产物、
量了被遮挡的区域、量了还没重新导入的缓存，然后基于假数据往下推一整轮。

下面每条都在本项目真实发生过，且都有「日志全绿 / 退出码 0」的外表。

## 一条总原则

**验证的对象必须是运行时真值，不是中间产物。**

磁盘上的 PNG 是新的、代码引用是对的、加载回调成功了 —— 这三件事**全部成立**
时，屏幕上依然可能是旧画面。必须一路查到「运行时实际渲染出来的值」。

## 退出码 0 ≠ 成功

| 现象 | 真实情况 | 怎么识破 |
| --- | --- | --- |
| Lovart `--download` 退出码 0、日志有 `[image] https://...` | **一个文件都没下载** | 查目标目录文件数/时间戳，别信日志 |
| `cutout.mjs` 打出 `816x816 -> 816x816` | 一个像素都没抠掉 | 输入输出尺寸相同 = 没生效 |
| 批量任务「10 秒跑完 15 个」 | 全部失败 | 按单个任务的合理耗时反推 |
| `powershell -File x.ps1` 立刻返回、日志文件 3 字节 | 子进程根本没跑起来 | 耗时明显短于正常值 |
| 生成脚本跑了 7 分钟、四张图都没有 | 图生成完了才在打印时崩 | 搜 `UnicodeEncodeError`，不搜关键词 |

**判据：拿耗时和产物数量对账。** 「跑完了」和「跑失败了」在日志上长得一样，
但耗时不会骗人。

## 不要用关键词白名单过滤输出

```powershell
# ❌ 会把你正要找的那几行一起滤掉
... 2>&1 | Select-String -Pattern 'OK|FAIL|generation_succeeded'
```

本会话踩了**两次**：

1. 过滤 `local_path|generation_succeeded|Error:` → 输出全空，
 判成「生成失败」。实际是**生成成功、只是 `--download` 没下载**，
 真实日志里写着 `Status: done` 和图片 URL。白跑一轮。
2. 用 `Select-String '竞品'` 过滤测量结果 → 四个脚本全部返回空，
 看起来像脚本没输出。实际是**中文在这个终端的编码不匹配**，数据一直都在。

**空结果和被滤掉的结果长得一模一样**，这是最容易骗过自己的地方。
正确做法：全量写进日志文件，再从文件里读。

```powershell
& node script.mjs 2>&1 | Out-File -FilePath run.log -Encoding utf8
Get-Content run.log -Tail 30   # 先看尾部，别先过滤
```

**中文关键词不要用来过滤**，这个终端是 GBK，匹配不上。

## Cocos 资源监听不捕获外部写入的文件

**用编辑工具/脚本覆盖 `assets/` 下的图片后，编辑器不会自动重新导入。**

症状：磁盘 PNG 是新的、大小对、时间戳新，但预览里还是旧图。
用户的反馈是「感觉没什么变化，图案还是没变」。

`Get-Item` 改 `LastWriteTime` 触发不了，等 60 秒也没用（实测）。

**查证方法：比 PNG 和 `library/` 缓存的时间戳。**

```powershell
$m = [IO.File]::ReadAllText("$root\assets\resources\map\alcoves\alcove_bath.png.meta")
$u = ([regex]'"uuid":\s*"([0-9a-f\-]{36})"').Match($m).Groups[1].Value
Get-ChildItem "$root\library\$($u.Substring(0,2))" -Filter "$u*.png"
```

`library/` 里的时间早于 PNG = **还是旧纹理**，此时截图测量毫无意义。

**唯一可靠的解法是让用户在编辑器里右键目标目录 →「重新导入资源」。**
不要为此杀掉正在运行的编辑器（可能有未保存的场景改动）。

工具见 skill `asset-change-verification`。

## 「加载成功」≠「看得见」

图标真图探针打出 `active=true`、`frame=icon_gift`、`rect=126x128` 全部正常，
但量出来的色相种类**仍然是 2/12**（和没换图一样）。

原因：渲染尺寸只给了 `ENTRY * 0.52`（约 29px），**多色细节被降采样吃掉**。

**两件事都要验**：资源加载成功、以及渲染尺寸足够大到细节留得住。
分区图同理 —— 512 长边的图塞进 152~202px 的框，`placeArt` 等比缩放后
**永远被高度卡住**，提示词里要的小物件全糊成几个像素。

推论：**出图之前先用 `probe-artsize.mjs` 读目标框的运行时尺寸**，
并把这个尺寸写进提示词。事后返工比事前算一次贵得多。

## 截图测量前先确认量的是什么

- **FPS 调试面板会盖住屏幕底部**。量底部按钮区时得到「彩色占比 71%、
 明度 63」这种离谱数值，量的是深色面板不是按钮。**数值离谱先看图**。
- **整图统计会稀释小面积问题**。按钮区色相种类 1/12 vs 竞品 8/12 时，
 整图彩色占比却已经和竞品持平（9.2% vs 9.8%）—— 因为墙和地板占了大头。
 小元素必须**单独框区域**量。
- **肉眼看原图看不出改动**。414×896 里一个壁龛只有 116×135px，
 必须用 `crop-zoom.mjs` 放大 3~6 倍、并把竞品同部位裁同倍数并排看。
 我曾靠「色相种类 4/12 → 6/12」判断生效，放大后发现画面几乎没变。

## 报根因之前，先确认读到的是文件真实内容

读文件工具会在内容里插入行号元数据（每 10 行一次，形如 `    10|`）。
**那不是文件里的字符。**

我曾把 `import-alcoves.ps1` 里的 `    10|$cut = ...` 当成残留前缀，
向用户报告「找到根因：脚本有 bug 导致产物写错位置」——**完全错误的结论**，
而且已经说出口了。真实原因是另一件事（生成的图和上一版长得太像）。

**说「找到根因」之前，先满足一条**：能指出一个可复现的现象，
并且这个现象在修掉之后会消失。仅仅「代码看起来不对」不够。

## 编码陷阱

- **只设 `PYTHONIOENCODING=utf-8`，不要设 `PYTHONUTF8=1`。**
 前者修 stdout 打 `✓` 时的 `UnicodeEncodeError: 'gbk' codec`；
 后者会改 argv 的解码方式，**中文提示词进去就碎**
 （`UnicodeDecodeError: 0xbb in position 224`）。
- **嵌套 `powershell -NoProfile -File x.ps1` 可能让子进程完全不输出**
 （日志 3 字节、退出码 1）。同样的命令写成**内联**就正常。
 别为此调试嵌套调用，直接写内联。
- 其余 PowerShell / 编码坑见规则 `cocos-toolchain`。

## 相关

- **量法本身的缺陷**（指标选错、直方图分不清笔画和描边、只调颜色不问该不该有）：
 规则 `visual-measurement-method`。本规则管「量错对象」，那条管「量法失效」。
- 出图侧的描边与背景纪律（含「深一到两档」这条已被推翻的结论）：
 skill `game-art-from-ai`
- 截图测量脚本与目标数值表：skill `ui-visual-parity`
- 确认**美术资源**改动生效的完整流程：skill `asset-change-verification`
- **代码画的 UI**（Graphics/Label/节点树）空白/不显示/点哪都跳：
 规则 `ui-runtime-verification` + skill `ui-runtime-probe`。
 本规则管「PNG 没上屏」，那条管「节点没画出来 / 没触发刷新」。
