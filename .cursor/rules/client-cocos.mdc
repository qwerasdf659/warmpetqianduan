---
description: Cocos Creator 客户端开发规范
globs: assets/**/*.ts
alwaysApply: false
---

# 客户端（Cocos Creator）规范

## 语言约定
- 引擎按 **TS 4.1 语义**编译，`tsconfig.json` 不参与构建，只影响 IDE 检查。
- 隐含 isolatedModules：重导出类型必须写 `export type`，不用 const enum。
- 引用类型一律 `import type`，避免打包器误判。

## 职责边界
- 客户端只做「表现 + 输入采集」，核心逻辑在后端（服务端权威）。
- 本地缓存只存非权威数据；任何「时长/冷却/收益」以服务端返回为准。
- 本地预测（属性衰减等）只用于视觉平滑，**不能拿预测值做业务判定**。

## 通讯
- 常规请求走 HTTPS REST（wx.request）。
- 实时用**原生 `wx.connectSocket`**（标准 WebSocket），**禁止用 socket.io-client**（微信小游戏跑不了）。
- 自行实现心跳、重连、消息编号。
- 平台差异走 `assets/scripts/platform/minigame.ts` 适配层，
  业务代码不直接碰 `wx.*`——否则编辑器预览（浏览器里没有 wx）跑不起来。

---

# 引擎 API 陷阱

以下都是本项目踩过的，构建期不报错、运行时才炸。

## Graphics 不是 Canvas 2D

可用方法只有：`moveTo` `lineTo` `arc` `ellipse` `circle` `rect` `roundRect` `close` `stroke` `fill`。

**没有 `arcTo`。** 圆角矩形用内置的 `roundRect(x, y, w, h, r)`，不要按 Canvas 习惯手拼。
调用不存在的方法时构建不报错，运行时抛异常且**绘制会停在出错那一行**——
表现为界面画了一半，很难一眼看出原因。

## Label 默认锚点是 (0.5, 0.5)

左对齐文字摆在左边距坐标上，实际会有一半跑到屏幕外；右对齐同理会被裁掉。

锚点要跟着对齐方式走：`left → (0, 0.5)`、`center → (0.5, 0.5)`、`right → (1, 0.5)`。
这样调用方传的坐标才是文字那一侧的边。

## 渲染顺序 = 兄弟节点次序

后加的节点画在上面。**懒加载创建的 Graphics 会盖住先创建的 Label**
（典型症状：按钮有底色但没文字）。

统一「**先挂完所有 Graphics，再挂所有 Label**」——既保证层级正确，也利于合批。

## 运行时创建的节点默认是 DEFAULT 层

`new Node()` 的 layer 是 `Layers.Enum.DEFAULT`（3D 层），不是 UI_2D。
UI 节点必须显式设 `node.layer = Layers.Enum.UI_2D`，
否则 3D 相机会把它们也算进可见范围。

## fitWidth 下可视高度随机型变化

`view.getVisibleSize()` 返回的高度**不等于**设计分辨率的 1280，
它由设备宽高比算出来，编辑器预览窗口矮的时候可能只有六百多。

纵向布局不能写死偏移：顶部自上而下排、底部自下而上排、中间吃剩余空间。

## 禁止只有副作用的 import

`import './x'`（不导入任何符号）打包器解析不了，运行时报
`Unable to resolve bare specifier '__unresolved_N'`，**整个模块起不来、全黑屏**。

需要触发模块副作用时，正常 import 它导出的东西即可——
ESM 保证被依赖模块先于依赖方求值。

## 继承内置 Error 要补原型链

TS 4.1 目标下 `class X extends Error` 会丢原型链，`instanceof` 静默失效。
构造函数末尾补 `Object.setPrototypeOf(this, X.prototype)`。

## 场景里引用脚本用 23 字符压缩 uuid

场景 JSON 里组件的 `__type__` 是脚本资源 uuid 的压缩形式：
**保留 5 位十六进制头 + 18 位 base64 = 23 字符**。

引擎 `decodeUuid` 文档里那个 22 字符形式（保留 2 位头）是另一套，用错会报
`Script "xxx" attached to "yyy" is missing or invalid`。
生成器见 `_research/gen-meta.mjs`，里面有对照引擎产物的自检断言。

## 相机默认曝光是按「正午全日照」标定的

Cocos 相机的默认曝光参数（F16 / 1/125 / ISO100）对应现实里约 10 万 lux 的
全日照。所以**平行光的 illuminance 不能按「太阳是 88000 lux」去填**——
那会让高反射率的物体亮面直接顶到 1.0 被截断。

我们的宠物毛色反射率 0.85，主光给 88000 时的后果不是「有点亮」：
毛流、反荫蔽、分区色全被削平，docs/06 §3.4 精心排的三档明度
（背景 0.90 / 毛色 0.85 / 地台 0.70）一起糊成一片。室内暖光场景给到
**48000 一档**才有余量，环境光按 0.16 的比值跟着降。

改主光时环境光必须等比跟着改。只降一个会改变比值：只降主光会让暗面被填平，
模型「塌」成一张贴纸，等于用另一种方式抹掉立体感。

> 这个 bug 藏了很久，因为自渲预览用的是 Blender 默认的 AgX 色彩变换，
> AgX 会把高光平滑滚降回来——**过曝在预览图里看不见，在引擎里是死白**。
> 见 `3d-asset-pipeline.mdc` 的对应条目。

## 静默退回的兜底值决定玩家实际看到什么

`PetStage.FALLBACK_MODEL` 这类兜底不会报错、不会有紫色占位、没有任何征兆。
后端 `species` 现在返回 `"default"`，认不出就走兜底——**所以那一个常量
决定了玩家看到的是哪只宠物**。

同类：`SPECIES_MODEL` 里列了几个物种，`assets/resources/models/` 就要有
几个 GLB。漏一个不会报错，只会让玩家养的猫长着狗的样子。

改这类兜底时要问一句「兜底之后玩家看到的是不是我以为的那个」。

## 场景全局配置的颜色是 Vec4 不是 Color

`director.getScene().globals.ambient.skyColor` 要的是**归一化 Vec4（0–1）**，
不是 0–255 的 `Color`。传错类型不会报错，但效果不生效。

## `useBakedAnimation` 必须在节点挂进场景**之前**设置

组件激活时会按当时的 bake 标志初始化动画状态，而引擎在预烘焙分支里
**直接跳过求值器的创建**（`skeletal-animation-state.ts`：`_doNotCreateEval = baked`）。
那一步只跑一次，事后再改标志只是换了采样函数，求值器不存在，采样等于空转。

prefab 里序列化的默认值是 `true`，所以顺序必须是：

```ts
const anim = pet.getComponent(SkeletalAnimation);
anim.useBakedAnimation = false;   // 先设
pet.parent = stageRoot;           // 再挂进场景
anim.play('idle');
```

症状极具迷惑性：`isPlaying === true`、`time` 正常推进、`wrapMode` 正确、
把动画幅度放大 8 倍也一样，骨骼纹丝不动，全程零报错。
这条查了五轮，最后是读引擎源码才定位到的。

## glTF 导入的动画默认播一次就停

`AnimationClip.wrapMode` 默认是 `Normal`，播完停在最后一帧，
一个周期之后看起来和静止完全一样。循环要在运行时显式设：

```ts
clip.wrapMode = AnimationClip.WrapMode.Loop;
anim.play(name);
anim.getState(name).wrapMode = clip.wrapMode;
```

## builtin-standard 的颜色属性叫 `mainColor`

不是 `albedo`——那是 effect 里的 shader uniform 名，不是材质对外的属性名。
传错只打一句 `illegal property name`，材质保持默认白色，很容易被当成灯光问题。

`roughness` / `metallic` 则是对的，可以直接传。

## 运行时只有被资源引用到的 effect 才会打进包

`new Material().initialize({ effectName: 'builtin-standard' })` 在没有任何
使用该 effect 的材质资源加载之前，拿不到 EffectAsset，做出来的材质没有有效 pass。
把它赋给 `MeshRenderer` 会在引擎内部抛 `Cannot read properties of undefined
(reading 'localSetLayout')`，堆栈离真正的原因隔了六层，完全看不出是加载顺序问题。

所以运行时建材质前要先 `EffectAsset.get()` 判空并给出可读的报错，
需要 PBR 材质的东西排在模型加载**之后**再建。

## 组件 onLoad 抛异常会静默中断宿主的构建流程

`addComponent()` 会同步执行 onLoad。如果宿主是「先挂组件、再建自己的 UI」的写法，
子组件一抛异常，宿主后面的代码全部不执行——表现为界面只剩一片底色，
而报错藏在控制台里毫不起眼。

**次要组件的 onLoad 要自己兜住异常**，不要让它有机会带走宿主。

## `setParent` 默认保留世界变换

配饰挂到骨骼挂点之后必须把局部变换归零，否则引擎会反算出一个抵消偏移，
配饰留在原地不动。配饰是按「原点在挂点接触面中心」建模的，局部变换为零才落位正确。

## 模型子资源的 resources 路径要多一层

`pet_dog.glb` 里的 prefab 是子资源，运行时路径是 `models/pet_dog/pet_dog`——
`.glb` 被剥掉，末尾再跟一层子资源名。写成 `models/pet_dog` 加载不到。

## UI 相机在 3D 相机之后渲染

任何铺满屏幕的 UI 底图都会把 3D 内容盖住。**3D 场景的背景只能待在 3D 层**，
做成一块贴在主体后方的四边形，材质用 unlit（图本身已经画好光影，
再吃一遍实时光会脏）。

## UI 布局和 3D 相机取景是两套坐标系

UI 按 `view.getVisibleSize()` 排版，随机型变化；3D 相机却是固定的。
两边各算各的，视口一矮主体就会被 UI 面板压住。

必须由 UI 把预留区域的中心显式告诉 3D 侧，相机按视锥换算出垂直偏移对齐。
反直觉的一点：**相机往下移，主体在画面里反而往上走**——
主体没动，是取景中心跟着相机降了下去。

---

# 性能与包体（详见 docs/06-UI设计规范.md）

- 主包 ≤ 4MB；**所有包合计 ≤ 20MB**（30MB 那档需开通虚拟支付，本项目铁律不接支付）
- 整屏 DrawCall ≤ 50（低端机）；同屏骨骼角色 1–2 只
- **骨骼 ≤ 30 根**（`JOINT_UNIFORM_CAPACITY`，超过会退化成贴图蒙皮，低端机明显掉性能）
- 3D 换装需关预烘焙走实时蒙皮（`useBakedAnimation = false`）
- 贴图**正方形且 2 的幂**（iOS PVRTC 硬性要求，非标尺寸转换后体积反而暴涨）
- 纹理压缩效果**必须真机验证**——开发者工具不支持 ASTC 会回退成未压缩，数据不可信

## 合批与重绘

- Cocos 按渲染顺序合批，中间插入不同类型组件会断批
- 不要用 RichText 的颜色标签渲染整页文本（每个 `<color>` 切一次批，一页能到 128 DrawCall）
- 每帧变化的内容先算「看得见的变化」签名（数值取整、冷却取秒），没变就跳过重绘

# 微信平台

- 好友关系链/好友榜走**开放数据域** + 微信托管 KV，不经过自有后端。
  开放数据域是独立渲染环境，访问不到主域变量，UI 要单独实现一遍。
- 顶部 UI 必须避让右上角胶囊按钮（`wx.getMenuButtonBoundingClientRect`）。
  胶囊坐标是屏幕物理像素，要按「可视尺寸 / 窗口尺寸」换算成设计分辨率单位。
