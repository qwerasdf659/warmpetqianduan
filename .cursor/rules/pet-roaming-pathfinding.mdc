---
description: 家庭地图宠物满地板 2D 游走 + 栅格 A* 寻路 + 长按拖动宠物的选型结论与踩坑
alwaysApply: true
---

# 家庭地图 · 宠物游走与寻路

家庭地图（`ui/MapView.ts`）的宠物从「只沿一条横线走」升级成「整个地板 2D 自由
游走、会绕开道具、可被长按拖动」。核心在 `ui/mapPathfind.ts`（栅格 A\*）+
`ui/mapPropMasks.ts`（自动生成的障碍遮罩）。视觉对标方法见 skill `ui-visual-parity`，
出图见 skill `game-art-from-ai`，本规则记选型结论与踩过的坑。

## 选型结论（已定，别再推翻）

- **宠物满地板 2D 游走用栅格 A\* 寻路**，不是随机横移。可走区是地板矩形
  （`petMinX/maxX/petMinY/petMaxY`），切成栅格、标障碍、A\* 找路、逐段 tween 走。
- **障碍从道具的「实际渲染节点边界」推导，不用布局表 `PROPS` 的目标框重算。**
  `placeArt` 会等比缩放，表里的 `w/h` 和缩放后的实际尺寸对不上 —— 用表重算
  会让障碍偏移（护理室上方两行标错就是这么来的）。读节点的 `position` +
  `UITransform.width/height`（缩放后真值），障碍才和画面对得上。
- **障碍形状 = 道具图 alpha 自动生成的遮罩（c_pixel），按整体挡。**
  离线脚本 `gen-prop-masks.mjs` 把每张 PNG 的 alpha 降采样成 20×24 的 0/1 网格，
  写进 `ui/mapPropMasks.ts`（直接 import，不读 GPU 纹理 —— Cocos 的 Texture2D
  默认不可读）。**面向后台运营**：运营丢新图进目录 → 重跑脚本 → 障碍自动精准，
  零代码改动、零手配。完整流程见 skill `map-pet-pathfinding`。
- **矩形框套不准不规则道具**，别用它一刀切。斜楼梯用矩形会把斜边外的空地也标红，
  底座/整体两种矩形近似都反复出过问题 —— alpha 遮罩才是终解。
- **膨胀用圆形（欧氏距离）不是方形（切比雪夫）。** 方形膨胀在对角方向多扩 √2 倍，
  是「相邻两个家具红区粘连、吃掉中间过道」的主因。圆形只扩必要量、留得出缝隙。
- **三种手势共存**：短按宠物 = 互动、长按宠物拖 = 移动宠物、按空白拖 = 平移地图。
  长按用 `update(dt)` 累计时长触发（`LONG_PRESS_SEC`）；手指先移动超阈值则取消长按
  待命、退回拖地图。拖宠物时停散步 tween、松手吸附到最近可走点（`snapToFree`）。
- **点宠物互动取代了底部互动条**（`MapActionBar` 已删）。做哪种互动由头顶气泡
  当前需求决定（饱食低→喂食/清洁低→洗澡/心情低→陪玩/都健康→抚摸），见
  `interactWithPet`/`currentPetAction`。冷却中**静默不响应**（没有常驻按钮需要
  解释「为什么点不动」，弹「还要等 Ns」反而突兀）。
- **HUD 是固定浮层，宠物可以走到按钮后面。** 竞品猫咖就是这么做的：HUD 挂在
  `this.node`（不随地图拖动）、在宠物层之后挂 → 天然盖在猫上、点击优先给按钮。
  所以猫的游走下沿放到接近屏幕最底（`-vh/2 + 40`），视觉遮挡无所谓 —— 地图能拖，
  想看被挡处拖一下即可。别为了「别挡住猫」把猫拦在 HUD 上沿。
- **宠物的所有附件跟猫的实时 x/y**（气泡、状态条、接地阴影、点击命中框、收益飘字）。
  猫现在满地板走、y 一直变，**别再用固定 `L.petY`** —— 那会让附件留在原来那条线上。

## 踩过的坑（每条都真实发生、且有「看起来正常」的外表）

### 编辑器编译滞后会骗过截图和探针（这轮栽了多次）

改完代码，编辑器编译**滞后甚至卡住**，此时截图/探针跑的都是**旧代码画面** ——
「护理室标记不对」查了半天，最后发现是旧编译。**判据：搜编译产物里有没有你这次
加的新符号/新注释**，不是看时间戳：

```powershell
$dir = "temp\programming\packer-driver\targets\preview\chunks"
Get-ChildItem $dir -Recurse -File -Filter *.js | Select-String -Pattern "你这次新加的独特字符串" -List
# 搜不到 = 还是旧编译，别急着改代码，先等编译 / 重新导入
```

用户反馈「标记不对」时，**先确认他看的是不是旧编译**，别直接改逻辑。

### 采样坐标靠肉眼猜 → 量错对象，数据自洽但结论错

竞品地板色、踢脚线**量错过多次**：
- 用 `color-compare` 默认区域取地板，取到了带阴影的暗部 → 得出「灰色 204,197,187」，
  照着把地板压暗又降饱和，越改越灰。**在竞品干净地板区多点取样**才发现是暖原木色
  248,235,220（又亮又暖）。
- 拿单个 x 竖扫墙地交界，正好落在壁龛地垫阴影上 → 误判「有 13px 深木踢脚带」，
  还推翻了正确的「竞品没踢脚板」结论。**多点复扫**（5 个 x）才确认普通交界没有。

**判据**：单点/单区采样得出的结论，先换几个位置复核；量出「灰/深」这种和直觉
相反的值时，先怀疑采样区域不对，别急着照着改。详见规则 `visual-measurement-method`。

### 探针 evaluate 里 `tr.width` 读到 nil，不代表运行时代码错

playwright `page.evaluate` 里 `getComponent(cc.UITransform).width` / `.contentSize`
可能取到 `undefined`（上下文 getter 问题），但游戏运行时代码是对的。
**别据此判定代码 bug** —— 改成在游戏代码里 `console.log` 打印真值，再用探针捕获
console（这才是运行时真值）。本项目靠这招确认了障碍框尺寸其实正常。

### 障碍范围反复在「太大」和「太小」之间摆

- 用**整幅图框**当障碍 → 高家具（台灯/爬架）竖着占一大条，猫困在中间。
- 改**只挡底座** → 柜子/护理室的柜体上半部没挡，猫走进柜子里。
- 用 `solid`/footprint 两种矩形分类 → 斜楼梯还是套不准、相邻家具还是粘连。

**别在矩形近似里反复调比例**，那是死路。终解是 alpha 遮罩（按真实形状）+ 圆形膨胀。

### 地板/背景要按世界宽 `worldW` 铺，不是屏幕宽 `vw`

打底背景色曾用 `vw` 铺、又挂在会随地图拖动的 `worldRoot` 下 → 拖到最左/最右时
背景边缘离开屏幕对侧，露出相机清屏的深色（「拖到边角有褐色缺口」）。
墙/地板本来就按 `worldW` 铺，打底色也要对齐。**背景铺满，内容避让**。

### 「可选回调里塞副作用」和「首刷只靠签名」两个老坑仍适用

见规则 `map-scroll-view` / `ui-runtime-verification`：产生副作用的调用别塞进
`if (可选回调)`；带签名去重的刷新要在建节点后主动刷一次 + 幂等兜底。
本次气泡首帧空白、道具节点 0 children 都是这两类。

### 细杆家具照实标会被猫穿过 → 障碍要有最小宽度

台灯的杆只占图宽 ~10%（遮罩里一行 `00000000011000000000`），照真实形状标的话
猫会从杆**两侧的空隙**钻过去，看着像「穿过台灯」。`blockByMask` 里对每一行：
有实体就以实体中心为准把宽度撑到至少 `MIN_BLOCK_W`（40px）。对所有细杆道具通用，
不用逐个配。判据：探针读台灯附近每行障碍格宽度，灯杆段应 ≥32px（不是一格）。

## 新道具的红区准不准（后台运营必看）

新道具（不在现有 6 样里）的障碍：

- **会不会被当障碍 = 自动。** `loadProps` 里任何 `on:'floor'` 且非 `flat` 的道具
  加载后自动进 `obstacleNodes` → 自动成障碍。平铺物（地毯/坐垫/食盆）要在
  `PROPS` 表标 `flat: true` 才不当障碍。
- **形状准不准 = 取决于有没有重跑遮罩脚本。**
  - 跑了 `gen-prop-masks.mjs`：扫目录**所有** PNG，新图遮罩自动生成 → 精准贴形状。
  - 忘跑：`PROP_MASKS[图名]` 为 undefined → `maskFor` 返回 undefined →
    寻路**退回矩形底座近似**（不崩、不漏障碍，但不规则道具会偏）。
- **标准流程**（skill `map-pet-pathfinding`）：图进 `resources/map/props/` →
  `PROPS` 加一行（平铺物标 `flat`）→ 跑 `gen-prop-masks.mjs`。
  想免掉「忘跑」风险，把脚本挂进构建流程。

## 关键参数（都在 `mapPathfind.ts` 顶部，改一处即可）

| 常量 | 含义 | 现值 | 备注 |
| --- | --- | --- | --- |
| `CELL` | 栅格边长(世界px) | 16 | 调细缝隙才表现得出来；越小越贵 |
| `CAT_RADIUS` | 猫碰撞半径 | 12 | 只需够猫身；太大堵死过道 |
| `MIN_BLOCK_W` | 障碍每行最小宽度(px) | 40 | 细杆家具照实标会被猫穿过 |
| 遮罩分辨率 | `gen-prop-masks.mjs` 的 COLS×ROWS | 20×24 | 加大更精细、TS 体积略增 |

游走范围（`mapLayout.computeLayout`）：`petMinX/maxX` 铺满世界宽（2%~98%），
`petMinY` 到接近屏幕底、`petMaxY` 到接近墙地交界。护理室/楼梯/家具由遮罩挡，
**不靠缩小游走范围去躲**。

## 调试网格（排查完必须关）

`MapView` 顶部 `DEBUG_GRID` 开关：画寻路栅格（蓝=可走 / 红=障碍 / 橙=猫当前路径）+
格子编号（左到右、上到下、从 1）。用户可按格号报「第 X 号该走却是红的」精准定位。
**上线前置 `false`**（注释里也写了）。
