---
description: Cocos 工程的构建、预览与缓存操作纪律
alwaysApply: true
---

# Cocos 工程操作纪律

以下每一条都在本项目实际造成过白屏或返工，不是理论建议。

## 绝不在编辑器打开工程时跑 CLI 构建

`CocosCreator.exe --project ... --build` 会启动**另一个 Cocos 实例**，
和编辑器抢同一个 `temp/` `library/`，编译产物互相覆盖。
症状是预览白屏 + 控制台报 `Unable to resolve bare specifier '__unresolved_N'`，
而且报错指向引擎自己的 `cce:/internal/x/cc`，很容易误判成代码问题。

要验证构建，二选一：

- 先确认编辑器已关闭（`Get-Process *Cocos*` 只剩 `CocosDashboard` 才算关干净）
- 或把工程复制到临时目录再构建，不碰工作副本

日常出包让用户在编辑器的**构建面板**里做。

## 类型检查不在构建流程里，必须单独跑

Cocos 构建**只转译、不做类型检查**。调用引擎上不存在的方法，
构建照样"成功"，运行时才抛异常、界面画一半停住。

改完 `assets/scripts/**` 必须跑：

```
node _research/typecheck.mjs
```

它按本机 Creator 安装位置解析出 `cc.d.ts`，生成一份本地 tsconfig 再调 tsc。
引的是编辑器自带的那份声明，不依赖 `temp/declarations`（那个要编辑器手动生成，
默认是空的——引它会让检查**静默通过**，比报错更糟）。

Creator 装在别处：`$env:WARMPET_COCOS = 'D:\CocosCreator\3.8.8'`。

**不要直接 `tsc -p _research/tsconfig.check.json`。** 那份基础配置里没有
`cc.d.ts` 那一项（它以前是硬编码的绝对路径，换台机器就跑不起来），
直接跑的话引擎类型全是 `any`，一样会静默通过。

## 预览坏掉时的恢复步骤

1. 关闭 Cocos Creator（Dashboard 可以留着）
2. 删除 `temp/` `library/` `build/`
3. 重新打开工程，等导入完成（全删后会比平时慢）

**不要删 `.meta` 文件**——资源 UUID 在里面，删了场景就找不到脚本，
所有组件会变成 Missing Script。

反过来说，**`temp/` 下不能放任何你还想要的东西**。资产管线的实验产物一律
写 `.build/`（见 `3d-asset-pipeline`），以前堆在 `temp/chibi`、`temp/eye`
这些目录下的中间模型，一直活在被这个恢复流程顺手删掉的风险里。

## 改引擎模块配置后必须重新导入

改 `settings/v2/packages/engine.json` 的 `includeModules` 之后，
编辑器要重新编译引擎。配置和已编译引擎不一致时，
引擎 barrel 模块会解析失败。

## package.json 的 version 是编辑器版本

Cocos 工程的 `package.json` 不是 npm 包描述文件。
`version` 和 `creator.version` 必须是 `3.8.8`；
写成应用版本号（如 `0.1.0`）会让 Dashboard 显示「缺失编辑器」打不开工程。

## WebGL 上下文有数量上限

多开几个浏览器预览标签，加上编辑器自己的场景视图，很快就会把上下文用光。
症状是画面突然变白 + 控制台 `CONTEXT_LOST_WEBGL`，编辑器也会弹
「WebGL context of the scene view has been lost」。

**同时只留一个预览标签。** 这不是代码问题，跟它较劲会白白浪费好几轮。

## 「网络异常」先怀疑假后端挂了

开发期连的是 `node _research/mock-server.mjs`（`net/config.ts` 的 `DEV_BASE_URL`
指向 `127.0.0.1:8899`）。它是个前台 node 进程，**关终端、重启机器就没了**，
而且不会有任何提示。

症状：游戏卡在「网络异常，请检查网络后重试」，控制台一片
`ERR_CONNECTION_REFUSED 127.0.0.1:8899`。

别从控制台一条条往回查，先看端口：

```
Get-NetTCPConnection -LocalPort 8899 -State Listen
```

起来之后 `GET /pet/state` 返回 **401 是正常的**——那个接口要带 token，
游戏会先走 `/auth/mini` 拿。能返回 401 就说明服务在正常处理请求了。

## 微信小游戏主包上限 4MB，资源必须走远程

**加资源时就要算包体**，别等构建时才发现。本项目「素材全部导入」后
`assets/resources` 到了 60MB+，构建报
`source size 63530 KB exceed max limit 4MB`，超了 15 倍。

- 上限是**主包 4MB**；分包（subpackage）总量上限 20MB，
  60MB 连分包也装不下 → **只有远程资源一条路**。
- `assets/resources` 已经是 Bundle（`resources.meta` 里 `isBundle: true`），
  配成远程**不用改任何代码**，`resources.load()` 调用方式不变。
- 配置在**编辑器构建面板**做（勾「配置为远程包」+ 填资源服务器地址），
  **别手改 `profiles/v2/packages/builder.json`**——那是编辑器的配置缓存，
  会被覆写，改错还会让构建异常。
- 完整步骤、Sealos 对象存储配置、白名单与验证顺序见
  `docs/小游戏包体与远程资源配置.md`。

几个必知的坑：

- **桶权限要 `publicRead`**。私有桶的对象要带签名，小游戏是裸 URL 下载 → 全部 403。
- **微信后台 `request` 和 `downloadFile` 是两个白名单**。后端 API 配前者、
  资源下载配后者；只配了 request 的症状是「API 正常但资源全下不来」。
- **开发者工具「不校验合法域名」会掩盖白名单问题**，本地能跑不代表真机能跑，
  必须取消勾选再验一次。
- **上传 `remote/` 时别多套一层**：传的是它的**内容**到桶根，
  正确路径是 `<bucket>/resources/config.json`，能在浏览器打开返回 JSON 才算对。
- **每次重新构建都要重传 `remote/`**。忘了传的症状是「改动没生效」，
  极易误判成代码 bug。
- 勾 **MD5 Cache**：文件名带内容 hash，内容变则 URL 变，
  绕开浏览器/微信/CDN 的缓存失效问题。不勾迟早撞上「传了但读到旧文件」。

减重优先级（都验证过）：删 3D 遗留 `models`/`materials` → `.json` 骨架换 `.skel`
（省一半）→ 压缩贴图。注意 PNG 对插画类压缩有限，缩到长边 256px 的缩略图
136 张也只从 9.02MB 降到 7.59MB。

## 打开的是构建产物目录，不是工程根目录

微信开发者工具报「未找到 game.json」时，先看打开的是哪个目录：

- `<工程根>/` — Cocos 工程（源码，给 Cocos Creator 开），**永远没有 `game.json`**
- `<工程根>/build/wechatgame/` — 小游戏包（构建产物，给微信开发者工具开）

另外产物可能是几天前构建的，看不到最新改动很正常——
先在编辑器构建面板重新构建，再回微信工具刷新。
对比 `build/wechatgame/game.json` 和 `assets/scripts/**` 的修改时间就能确认。

## 预览页要强制刷新

换了模型或贴图之后按 `Ctrl+Shift+R`。普通刷新会复用缓存里的旧资源——
文件名没变、内容变了，浏览器不会重新下载，改动等于没生效。

## gen-meta.mjs 会重写 main.scene

`_research/gen-meta.mjs` 不只是补 `.meta`，它把 `assets/scenes/main.scene`
整个文件重新生成一遍。**在编辑器里对场景做的任何修改，跑一次这个脚本就没了。**

跑它之前先确认 main.scene 没有想保留的改动。

## npm 官方源在国内会超时

`registry.npmjs.org` 经常 `EIDLETIMEOUT`，尤其是装 `@cocos/creator-types`
这种几万文件的大包。统一加国内镜像：

```
npm install --registry=https://registry.npmmirror.com
```

## Windows / PowerShell 注意

- **跑项目脚本前先确认 cwd**。终端的工作目录可能不在工程根
  （工作区路径变动后会跑到 Cursor 程序目录），症状是
  `Cannot find module 'C:\...\Programs\cursor\_research\mock-server.mjs'`。
  给命令显式指定工作目录，别假设 cwd。
- `curl` 是 `Invoke-WebRequest` 的别名，要用真 curl 得写 `curl.exe`
- 传 JSON body 时 PowerShell 的引号转义很容易出错，优先写进临时文件再 `--data-binary "@file"`
- **不要用 `node -e` 传含引号的脚本**。PowerShell 会吃掉反引号和双引号，
 报的错还指向被截断后的乱码，完全看不出是转义问题。
 改文件用编辑工具，非要跑脚本就先写进 `.mjs` 文件再执行。
- **参数开头不能是 `*` 之类的特殊字符**。PowerShell 会把整个参数吃掉，
 报 `argument --xxx: expected one argument`。踩过一次：提示词以
 `**不要...` 开头，批量 15 张图全部失败、10 秒内退出，
 而报错完全看不出是开头那两个星号。对策：加一个前导空格兜底。
- **批量任务先单跑一个验证。** 10 秒就「跑完」15 个 = 全部失败，
 跑完再看等于白等几分钟。跑完还要**逐项查 exit code** ——
 中间个别失败不会中断后续，只看最后几行会漏掉。

### 带中文的 `.ps1` 必须存成 UTF-8 **带 BOM**

PowerShell 5.1 读没有 BOM 的 `.ps1` 时按**本机 ANSI 代码页**解码，
UTF-8 的中文会碎成乱码，然后解析器在某个半个字符的地方报
`字符串缺少终止符` 或 `缺少右“}”`——**报错位置和真正的原因毫无关系**，
盯着那一行改半天也改不对。

同一个坑还有第二个形态：`Get-Content xxx.json -Raw | ConvertFrom-Json`
也是按 ANSI 解码，中文注释一乱，JSON 就断在半截字符串上，
报一个 `ArgumentException`，同样看不出是编码问题。

两条对策：

- 写完 `.ps1` 存成 UTF-8 with BOM
- 在脚本里读文件用 `[IO.File]::ReadAllText($p)`（.NET 默认 UTF-8 且认 BOM），
  不要用 `Get-Content`

### 表格输出会被吞成空白

`Get-ChildItem | Select-Object Name, Length` 这类**输出对象表格**的命令，
在这个环境里经常只返回空白——不是没结果，是表格没被渲染出来。
连着撞过三次，每次都以为文件不存在。

自己拼字符串就不会：

```powershell
Get-ChildItem x -File | ForEach-Object { "$($_.Length)`t$($_.Name)" }
"reachable=$(Test-Path 'C:\path\to\file')"
```

### 过滤长输出时别用关键词白名单

`... 2>&1 | Select-String -Pattern "PASS|FAIL|tris="` 这种白名单会把
**你正要找的那几行**一起滤掉。我因此漏看了 `inspect_fbx` 打的贴图清单，
得出了「这个模型没有贴图」的错误结论，还照着这个结论往下推了一整轮。

要么用 `-Context` 带上下文，要么先看全量输出再决定滤什么。
**空的结果和被滤掉的结果长得一模一样**，这是最容易骗过自己的地方。

### argparse 的负值参数要写等号形式

`--forward -y` 会被 argparse 当成两个选项，报 `error: argument --forward:
expected one argument` 然后退出码 2、没有任何有用信息。写 `"--forward=-y"`。
