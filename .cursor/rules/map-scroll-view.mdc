---
description: 家庭地图（MapView）的选型结论、布局约束与所有踩过的坑
alwaysApply: true
---

# 家庭地图 · 单屏房间 + 横向平移

实现拆四个文件：`ui/mapLayout.ts`（布局+配色，**所有数值的唯一来源**）、
`ui/mapRoom.ts`（环境与装饰）、`ui/mapZones.ts`（分区绘制）、
`ui/MapView.ts`（组装+Spine 猫+命中+HUD）。风格基准 `docs/地图概念稿-00-仿竞品布局.png`。

视觉对标的**方法与校验脚本**见 skill `ui-visual-parity`；本规则只记结论与约束。

## 选型结论（已定，别再推翻）

- **横向可拖（世界 1.6 屏），纵向不滚，不做等距。** 竞品实机截图证明可横向平移
  （一张显示「护理室+窗户」、另一张显示「洗浴间+花园+育婴室+楼梯」，
  是同一房间的两个位置）。纵向滚会破坏「墙在上、地在下」的稳定构图；
  等距要重写寻路/遮挡/朝向，和既定的 2D 平面 `PetStage` 直接冲突。
- **世界宽 = 1.6 屏，不是 3 屏。** 保留「一屏看到 2~3 个分区」的密度。
  曾用 2160×3600（3 屏）的大世界，一屏只能看到约 12% 面积、
  分区之间隔着上千像素空地板，每个分区都孤零零占满整屏。
- **墙占屏 23%、地板占大头。** 这个数字被顶部 HUD 厚度卡着（见下）。
- **宠物不跟视口**，在世界中段（`petMinX`~`petMaxX`，约 20%~80%）自由游走，
  拖到两端可能看不到它 —— **预期行为**。它是「住在房间里」，不是「贴在屏幕上」。
  别为了「随时看到宠物」把它绑到视口中心，那会毁掉刚建立的空间感。
  真要引导，正确做法是给个「回到宠物身边」按钮移动**视口**。
- **一图多用**：`PropSlot.art` 省略时等于 `key`，绿植/坐垫摆多处只用一张图。
- **代码绘制层不删**，它是图加载失败时的兜底轮廓 ——
  空墙和「图还没到货」看起来一模一样，排查时会白走一轮。

## 布局的硬约束

### 墙占比的瓶颈是 HUD 厚度，不是墙

| | 墙 | 地板 | 顶部 HUD |
| --- | --- | --- | --- |
| 竞品（两张截图都是） | 23% | 77% | **6%** |
| 我们（改前） | 47% | 53% | 19% |
| 我们（现在） | **23%** | 61% | 8% |

**连查三轮都在调墙本身，方向是错的。** 真正的约束是：分区壁龛连同招牌必须
塞进「HUD 下缘」到「墙地交界」之间。HUD 吃掉 19% 时，23% 的墙只剩 31px
放壁龛（需要 110px），墙被迫留到 45%。**减薄 HUD 才是前提**，四步压到 8%：

1. 头像 56 → 44 → 40
2. 等级徽章：竖排在头像下 → 横排在头像右 → **压在头像下沿上**（overlap 不占高度）
3. 经验条：12px + 条内数字 → 挂在货币条下方的 **7px 细线**、不带数字
4. **左侧不避让微信胶囊**（省 40px）

### 左上角的 UI 不需要避让微信胶囊

`topInset()` 返回「胶囊**底部** + 8」，把 UI 整个推到胶囊下方。
但胶囊固定在**右上角**，左上角的头像/货币条完全可以和它并排 —— 竞品就是这样。
我们曾对整个 HUD 用 `topInset()`，白让出 60px（屏高 7%），
**比竞品整个 HUD 还厚**。左侧元素用 `topInsetLeft()`（只避状态栏），
只有横跨整屏宽度的顶部元素才用 `topInset()`。

> `computeLayout()` 的 `top` 必须和 `MapHud.topY()` 用**同一个**基准函数，
> 否则内容上沿会和 HUD 实际位置错开。

### 顶部只放金币，积分挪到底部

金币和积分原本并排在顶部，右边那条伸到屏幕中线以外。现在顶部只留金币
（占左侧 29% 以内），积分放进「任务条右端 → 右下角入口左端」那道 237px 空档。

**两个池物理隔离是铁律**，拆到上下两处之后色相区分更要保住
（金币暖金 `COLOR.coin` / 积分冷紫 `COLOR.point`）。

`buildPointBar` 在空档不足 60px 时会**静默 return**（挤在一起比少一条更糟），
所以空档宽度由调用方按实际布局算出传进来，并用 `check-hud.mjs` 校验。

### 背景铺满整屏，内容避让 HUD

`top` 和 `wallTop` 是**两条不同的线**，别混用：

| 字段 | 值 | 用途 |
| --- | --- | --- |
| `top` | 屏幕顶 − 状态栏 − HUD 高 | **内容**排布上沿（分区、挂件、提示文案） |
| `wallTop` | `vh / 2`（屏幕顶） | **背景**上沿，铺满不避让 |

墙纸曾从 `top` 开始画，顶部留下一条 164px 纯米色空带（占屏高 19%），
HUD 浮在空带里。一般规律：**背景铺满，内容避让**。

## 三条必守的写法

1. **世界坐标用 `L.worldW`，不是 `L.vw`。** `PROPS` 的 `rx` 是世界比例；
   用 `vw` 会让右半边道具挤到中间、拖动后右侧露出底色。
   会跟着滚的内容挂 `WorldRoot`，HUD/提示/返回按钮挂 `this.node`。
2. **命中检测的数组顺序是语义。** 大厅（`shape: 'open'`）铺满整个地板、
   和其它分区重叠，必须排在 `zones` **最后**，取第一个匹配才等于
   「具体分区优先于大厅」。加分区要插在大厅之前。
3. **招牌必须常驻。** 分区图**不含招牌**（图里的字号/字体控制不了）。
   把 `paintSign` 写在 `paintAlcove` 里 → 它落在兜底层 → 图到齐后兜底层一隐，
   **木牌消失、只剩一行字飘在空墙上**。牌面 + 文字**打包成一个节点**
   挂在常驻的 `signLayer`（见 `MapView.buildSigns`）——
   牌和字必须同层，否则隐藏时会一半消失一半留下。
   招牌位置还要 **clamp 到 HUD 下缘之下**：壁龛高度调过多轮
   （110→174→268→201），不 clamp 会把招牌甩到屏幕外。

## 静默失败的三种形态（都踩过）

- **输入层 `contentSize` 为 0** → 收不到点击，但**拖动照样能用**
  （靠 `TOUCH_MOVE` 的 delta，对尺寸不敏感），症状是「能拖但点不动」。
- **`if (cb) cb(doWork())`** → 回调可选时副作用**从未执行**。
  `mapArt.placeArt` 曾这么写，而所有调用方都不传回调 → `mount()` 一次没跑：
  图加载成功、回调正常回来、日志打出 `frame=true`、零报错，
  但 `SpriteLayer` 一个子节点都没有。查了整整一轮。
  **产生副作用的调用绝不能塞进 `if (可选回调)` 的实参里。**
  排查手法：**数子节点**。`SpriteLayer: 0 children` 配上 `cb ... frame=true`
  这两条摆在一起才定位到，只看日志会以为一切正常。
- **静默 `return`**（如空档不足就不画积分条）→ 元素悄悄消失、无人察觉。
  这类分支必须配一个校验脚本兜住。

## 交互与命中

`e.getUILocation()` 返回**左下角为原点**的 UI 坐标。按「屏幕中心为原点」手算
会整体偏移半屏，**症状是「点屏幕任何高度都命中同一个分区」**，且不报错。

```ts
// 转到**世界节点**的局部空间，这样拖动之后命中依然正确
const local = worldRoot.getComponent(UITransform).convertToNodeSpaceAR(new Vec3(uiX, uiY, 0));
```

转 `this.node` 就错了 —— 那是不动的层，拖过之后会整体偏掉。
区分拖动与点击用位移阈值（累计 `|dx|+|dy| < 12` 算点击，和 `panelKit` 一致）。

**撞边界必须给反馈**（顶部文案临时变「已经到最左边了」）。手势有输入但画面不动、
毫无提示的话，玩家的判断是「地图坏了」而不是「到边了」。

**初始位置不要贴在边界上**：贴边时往那个方向拖完全没反应。
现在起始 x 取 `maxX * 0.45`，左右都能立刻拖动。

## 改一处会连带三处

改墙高 → 地板变高 → **按 `floorH` 比例算的尺寸全部变大**。
`stairH = floorH * 0.95` 曾让楼梯长到 436×451、一件家具占掉半屏。
这类尺寸一律要有**绝对上限**（`Math.min(200, floorH * 0.62)`）。

分区加宽 → 墙缝变窄 → 挂件放不下。分区从 150/190/156 加宽到屏宽
27%/32%/28% 后，中间两条缝只剩 14~18px，连挂钟都放不下，
三件挂件只能全挪到左右两端。用 `wall-gaps.mjs` 算，别试。

**挪坐标挪到第三次就该算。** 「修好 A 撞到 B」的循环
（楼梯压木马 → 挪木马 → 木马撞爬架）用 `free-space.mjs` 一次算清：
实测后排想放五件需 380px、实际只有 368px，所以必须移走一件。

**改 `mapLayout.ts` 的数值后必须重跑校验脚本** —— 它们内有同名常量副本，
不同步就是查了个假的。

## 「入口感」的三个特征

用户反馈「像摆在地上的独立元素」而不是「入口」时，查这三条：

1. **底部要越过墙地交界**。实测竞品地垫下沿只比交界低 **18px**（屏高 2%），
   壁龛主体仍在墙里 —— 是「门槛伸出来一小截」，不是「整个壁龛坐到地板上」。
   `ZONE_SINK` 在这个值上错过两次，方向正好相反：
   `80` → 整个壁龛被抬到交界线**上方** 62px、悬在半空；
   `0` → 底边正好贴墙裙、一点不越界，读成「全部镶嵌进墙」。
   注意 `zoneFloorY = wallBottom + SKIRT_H - ZONE_SINK`，SINK 要先抵掉墙裙。
2. **轮廓不能四周完整可见**。首尾俱全的东西只能读成家具，画得再精细也没用。
   **被裁切 = 空间在画面外延续** —— 楼梯推出屏幕右侧（溢出 62px、可见 72%），
   这是「上面还有一层」的**唯一**来源。
   ⚠️ 缩小 25% 后壁龛顶部够不到屏幕顶，「顶部被裁」这个特征失去了。
   两个特征在当前尺寸下**互相冲突**，只能保一个，选了「底部越界」。
3. **分区之间不留大片空墙**。竞品是「墙上一排连续的门」，间距 0.18 而非 0.20。

## 尺寸：加宽不会让图变大

`placeArt` 等比缩放 `scale = min(框宽/图宽, 框高/图高)`。壁龛图是竖的
（478×512），所以**永远被高度卡住** —— 把框从 150 加宽到 194，
图只从 75×80 变成 80×86（大 7%），玩家看不出任何变化。

要三个分区缩放一致，得把宽度给宽一点、让它们统一由**高度**限制；
否则各自限制点不同（育婴室图更窄 452 vs 478），缩出来是 26%/27%/30%、大小不齐。

## 平铺花纹与装饰

- **通道差留 10~14**。地板菱形格曾用通道差 7/11/17，代码跑了、DrawCall 也花了，
  **屏幕上是一片纯色**。低于 8 等于没画，高于 20 一整面墙读成塑料布。
- **装饰密度不是可选项**。概念稿有约 15 个装饰元素（窗+窗帘、相框、地毯、花盆、
  气球、木马、楼梯栏杆），旧版一个都没有，画面读成「几个色块摆在空地上」。
  纯 Graphics、零包体，成本只有几次 fill。
- **接地阴影**：每件落地家具和宠物脚下一片很淡的扁椭圆（`contactShadow`，
  透明度仅 30/255），把物件「焊」在地上。没有它所有家具都像贴纸浮着。
  阴影层**必须排在 SpriteLayer 之前**，否则会盖在家具上面。
- **图标别用 emoji 字符**。`⚙ ♪ 👥 📷` 在小屏上读成「一排灰圆圈」，
  更要紧的是**字形由系统字体决定，安卓低端机可能整个缺字**。
  用矢量简笔（`ui/mapIcons.ts`），零包体且必然出画面。
  每个图标给**专属色相**并和功能语义对齐（商店暖红、挑战奖杯金、领取礼物粉）——
  全染成同一个 `COLOR.accent` 会让「有彩色像素」只占 7.2%（竞品 21.6%）。
  但**饱和度要克制**，第一版给太艳导致占比冲到 15.5%、画面吵。

## 判断改动有没有生效

编辑器编译经常滞后甚至卡住，**产物里搜到新常量也不够**（可能只有部分 chunk 更新）。
最可靠的是 `probe-artsize.mjs` 读运行时的分区框尺寸。
本项目多次出现「产物含新常量、运行时仍是旧值」。

编译卡死超过 10 分钟：关闭 Cocos Creator → 删 `temp/`、`library/`、`build/`
→ 重开等导入。**不要删 `.meta`**（资源 UUID 在里面，删了场景会找不到脚本）。

## 分区 ↔ 玩法接口对照（接口都已封装，缺的是界面）

| 分区 | 玩法 | 接口 |
| --- | --- | --- |
| 大厅客厅 | 出战宠互动 + 家园装修 | `pet.feed/bath/pet/play`、`home` |
| 洗浴间 | 洗澡 | `pet.bath` |
| 护理室 | 消耗品 | `items` |
| 育婴室 | 扭蛋 / 新宠 | `gacha`、`pet.create` |
| 商店 | 商店 / 换装 | `items`、`wardrobe` |
| 花园阳台 | 赛跑 / 兑换 | `race`、`exchange` |
| HUD 弹窗（不占楼层） | 签到 / 任务 / 图鉴 | `daily`、`dex` |

**每个分区都要有出口。** 从地图进玩法后必须能回来 —— 主界面左上角
「← 小屋」按钮就是补这个漏洞加的；只有隐藏长按手势等于玩家出不去。

## 美术资源落位

- 分区门面：`resources/map/alcoves/alcove_<key>.png`（长边 512），`Zone.art` 填图名
- 环境道具：`resources/map/props/prop_<name>.png`（长边 384）
- `map/huts/*.png` 是更早的「独立店面」形态，**已不被引用** ——
  独立店面并排会读成商业街，不是家

出图流程见 skill `game-art-from-ai`（含「不要写深棕色描边」等提示词纪律）。
