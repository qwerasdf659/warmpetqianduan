---
description: 首页/加载界面的设计定稿与实现约束（改 Main.ts、main.scene、启动流程、首屏资源、或产出设计稿前必读）
alwaysApply: true
---

# 首页与加载界面

**设计定稿：`docs/首页设计稿-8-定稿-进度条版.png`**（2026-09-07 定）。
两张规格表（`宠物花色表-猫狗各5款.png`、`狗狗耳型对比-垂耳半垂立耳.png`）也在 `docs/` 下，
命名规范见 skill `design-mockup-archive`。

**迭代过程 1–7 号和备选版已于 2026-09-14 删除**（8 张共 17 MB，结论都已写进本规则）。
需要回看某一版：`git log --diff-filter=D --name-only -- "docs/首页设计稿-*.png"`
找到删除点，再 `git show <commit>^:<路径> > <输出>`。

## 定稿结论（已定，别再推翻）

- 游戏名 **暖宠小屋**（原「暖猫小屋」，加狗之后改）。中文名只出现在界面文案和提审信息里；
  代码里 `net/config.ts` 的 `warmpet_*` 存储键是内部标识，**不要跟着改**——
  改了会让老玩家已存的 token 和冷却记录失效。
- 首页是**一张静态宣传立绘**，不是 Spine 舞台。人物用正常日系比例（约七头身），
  猫狗用 Q 版。**渲染语言必须统一**：同样的手绘赛璐璐、同样的描边粗细、同一光照方向。
  头身比不同没关系，渲染方式不同才会读成两张图拼的。
- **人物只在首页出现**，静态、不绑骨、不进 Spine。玩法界面只有猫狗。
  这条是成本决定：正常比例人物的 Spine 拆件和动画比四足贵一个量级，而首页是全游戏
  唯一"玩家会停下来看几秒"的地方，静态图足够。
- **加载走进度条，不给 START**，读完自动进落地界面。
  落地界面现在是**家庭地图**（`ui/MapView.ts`，见规则 `map-scroll-view`），
  不再是宠物互动主界面——互动界面改由地图上的「大厅客厅」分区进入。
- 四个角标卡片（七日登录 / 温馨小屋 / 萌宠相册 / 新手礼包）是**纯展示**，不接真入口。
  所以别给它们箭头之类"可点"的视觉暗示——玩家戳了没反应就是
  `spine-2d-pet` 那条「静默失败 = 设计缺陷」。
- 狗用**小立耳**，猫狗靠「深色圆鼻头 + 略长吻部 + 张嘴吐舌」区分，不靠耳朵。
  耳型选型的完整取舍见 `spine-2d-pet`。

## 进度条和 START 不能同屏

两者表示互斥的状态：进度条说「还在加载，别点」，START 说「好了，可以点」。同屏的话要么
START 是灰的（点了没反应），要么 START 能点（进度条没意义）。**同一位置、两个阶段**才对。

唯一成立的"两者都有"是它们表示**不同的东西**：START 表示「首屏已就绪、可以进」，
下方细进度线表示「远程 Asset Bundle 还在下载」。等真把 Spine 和花色搬上 CDN
（见 `docs/小游戏包体与远程资源配置.md`）再考虑，现在没有远程分包，不要提前做。

## 进度条实现要点

**渐近补间比进度准确重要得多。** 玩家判断「卡死了」靠的是数字有没有动，不是数字对不对。
每个阶段给一个天花板，阶段内持续朝天花板爬（渐近、永远差一点），阶段完成就跳到天花板
并开始爬向下一档。**永不回退、永不停在同一个数字上。**

按 `core/bootstrap.ts` 的真实开销加权，不要平均分：

| 阶段 | 代码位置 | 区间 |
| --- | --- | --- |
| 对表 | `syncClock()` | 0 → 8 |
| 登录 | `ensureLogin()` | 8 → 25 |
| 补偿未确认操作 | `reconcilePendingOps()` | 25 → 40 |
| 加载宠物 | `api.pet.state()`（触发 `onReady`） | 40 → 70 |
| 并发补齐数据 | `Promise.all` wallet/offline/daily/home/list | 70 → 92 |
| 远程资源（暂无） | 将来的 Asset Bundle | 92 → 100 |

百分比印在条内居中，不放条外——底部合规块很挤，这样省一行。

## 文案：两行，分工不同

竞品验证过的做法是一行安抚 + 一行可核对（「游戏资源加载中，请耐心等待」+
「当前正在加载:基础资源(2/9)」）。照此：

- **主文案**用游戏自己的世界观词，如「小屋正在开门，请稍等…」。不要写「服务器」。
- **细节行**给真实阶段 + 计数，如「正在唤醒小猫 (4/6)」。它证明进度是真的在推进。

`bootstrap` 的 `onProgress` 现在回传的是「对时中 / 登录中 / 同步进度 / 加载宠物 /
加载数据」——**这些是给开发看的词，不要直接显示给玩家**。

## 清缓存按钮：必须白名单

「长时间无法进入，可尝试清理缓存」值得做（加载会失败，要给出路），但清理动作
**绝不能清 `PENDING_OPS_KEY`（`warmpet_pending_ops`）**。

那是未确认幂等操作的日志，`bootstrap.reconcilePendingOps()` 靠它做补偿重放。
玩家买东西时断网、然后点了清缓存，这笔钱就再也找不回来了。

可以清的：`warmpet_token`、`warmpet_user_id`、`warmpet_cooldowns`（重登、重新对冷却都无害）。

另外这个提示应当**延迟出现**（加载超过约 8 秒才淡入）。从第一秒就写着
「长时间无法进入」会吓到玩家。

## 布局约束

- 纵向一律**顶部自上而下、底部自下而上、中间吃剩余**。`fitWidth` 下可视高度随机型变，
  编辑器预览可能只有六百多，写死偏移会让标题和进度条撞在一起。同 `MainView.computeLayout()`。
- **合规块会吃掉屏幕下三分之一**（忠告 + 著作权 + 出版物号那几行）。立绘构图必须知道
  这一点，关键元素不要放在底部——对应 `docs/06-UI设计规范.md` §6.5。
- 合规内容**先只留位置和行数**（三到四行，14 号 `dim` 色）。走备案还是版号，两者要展示的
  内容不一样，等流程走完再填，布局不用返工。
- 失败态不弹窗、不黑屏：原地把进度条换成一行 `danger` 色人话 + 一个真的重试按钮，
  下面挂一行更小的清缓存提示。猫继续播 idle，界面还活着玩家就不焦虑。

## 首屏资源规格

- 首屏立绘是**首屏必需**，必须进主包。主包上限 4 MB，当前 2.09 MB。
- **必须出 1024×1024 正方形**。iOS 的 PVRTC 要求 2 的幂且长宽相等，
  9:16 竖图直接进包会被强行拉成正方形、体积暴涨（`docs/06-UI设计规范.md` §4.3）。
  按「中间那块 9:16 是安全区」构图，四周留出会被裁掉的余量。
- `docs/` 下那些 9:16 设计稿是**给人看的参考，不是可入包资源**，别直接拷进 `assets/`。

## 已知坑

**`assets/scenes/main.scene` 的相机清屏色是近黑 `rgb(28, 26, 36)`**，而定稿底色是
奶茶米色 `#F2E4D0`（`widgets.ts` 的 `COLOR.bg`）。这是深色方案时代的遗留，两边没同步，
**结果是玩家看到的第一屏是黑的、和整个游戏调性完全相反**。改配色时这两处必须成对改。

**图像模型生成的设计稿里，合规信息和互推位内容都是它编的。** 著作权人、软著登记号、
ISBN、审批文号、以及四个角标上的文案，全部是占位，不要当成真实信息往提审材料里抄。

**`Main.ts` 的 `showLoading()` 目前只有一个 Label 写着「启动中…」**，是当初为了让流程
跑通写的临时占位，不是产品形态。本规则描述的是目标形态，实现时以定稿图为准。
