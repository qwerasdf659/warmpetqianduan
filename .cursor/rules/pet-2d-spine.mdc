---
description: WarmPet 宠物 2D Spine 动画的选型结论与踩坑清单（3D→2D 迁移后长期有效）
alwaysApply: true
---

# 宠物 2D Spine 动画：结论与坑

## 选型结论（已定，别再推翻）
- 宠物用 **2D Spine 骨骼**，不是 3D、不是序列帧。runtime 用 **Spine**。
- **两套骨架**：拟人主宠（正面，养成核心：喂/洗/摸/玩、成长、心情、换装、技巧）+ 四足小宠（收集/环境/赛跑，猫狗共用一套）。
- 同体型内大小/胖瘦/头身比差异 = 调骨 + 换部件图 + 缩放 + Spine skins 复用；只有身体结构变（四足↔拟人）才另起骨架。
- 3D 管线（Tripo、`tools/` Blender、`*.glb`）已退役，别再往前推；概念图保留在 `assets-src/concept/`。
- 完整迁移/接入说明见 `docs/2D-Spine迁移与接入.md`、`docs/06-UI设计规范.md` §3.4/§5。

## 资源约定（代码按此加载）
- 落位 `assets/resources/spine/<name>/`，`<name>` = `pet_humanoid` / `pet_cat` / `pet_dog`（见 `PetStage.ts` 的 `SPECIES_SKELETON`）。
- 导出 Spine **4.2**：`skeleton.json` + `.atlas` + `.png`。
- 动画名至少 `idle`（循环）；主宠加 `happy` 等（`PetStage.react()` 会叠播 happy 再回 idle）。换装挂点用 Spine slot，`socket_` 前缀。

## 工具坑
- **God Mode / Stretchy / spine-animation-ai 只认人形** → 四足必须手工在 Spine 里绑（参照官方 Raptor 示例：脊柱分段 + 每腿两骨 IK + 尾/耳）。
- **God Mode 不是 Spine 的替代**：它只是出 Spine 文件的加速器，发布仍需买 **Spine license**、精修仍需 Spine 编辑器。换装/skins/网格需 **Pro（约 $379）**；公司年营收超 $50 万美金需 Enterprise。
- 用 God Mode 要进 **`ai-spine-animation`** 页（骨骼），不是 `ai-sprite-generator`（那是序列帧）。
- **DragonBones 已荒废、Windows 端导出被封**，弃用。
- 不用盗版/破解 Spine（合规红线，且破解版也不给发布权）。

## 绑骨底图要求（否则 AI 拆件出来四肢又短又碎、易脱节）
- 主宠：正面、对称 A-pose；**双臂外张、和身体留明显空隙**；手/爪画大画清；四肢分段明确、略加长；两腿分开。
- 四足：标准侧视，四腿 + 尾摊开、脊柱水平。
- 通用：**无投影/无阴影**、平涂描边、纯色底、留白边、全身入画。可用 skill `pet-rig-assets` 一步产出。

## God Mode 操作顺序（错一步就卡住）
1. 上传底图 → 勾 **Auto-remove background** → 点 **Build rig**（**必须先建骨架**，渲染按钮在建骨架前是锁死的）。
2. 拆分校验：拆件复选框是**标记「重做」用的**，拆对了别勾；错了拖关节点 Adjust cut 或单独 Redo。
3. 次级运动：**尾巴自动勾上是对的**（要摆），眼/耳/装饰别勾。
4. 选模板：挑**视角 + 体型接近**的——正面猫配**正面、双手垂放**的 idle；避开带披风/武器/长裙/侧身的（套上必拧）。
5. 渲染：正面不勾 Flip；商业 IP 不勾「公开分享」（走私有扣点）。同一角色重渲免费，多挑几版。
6. 导出选 **Spine 4.2**。

## 引擎与代码
- `settings/v2/packages/engine.json` 的 `includeModules` 已含 `spine`（option `spine-4.2`）；**改过 includeModules 后必须在编辑器重新导入引擎**，否则白屏（见 `cocos-toolchain` 规则）。
- `PetStage.ts` 是 2D 版：`sp.Skeleton` 播 idle，资源缺失退回 Graphics 占位（不白屏）；**别重新引入 3D 相机/灯光/材质**。
