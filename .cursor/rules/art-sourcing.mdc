---
description: 美术资源从哪来——采购渠道、授权红线、挑选标准，以及和脚本管线的分工
globs: assets-src/**,tools/inspect_source.py,tools/build_pet_from_source.py
alwaysApply: false
---

# 美术资源的来源与分工

## 先认清瓶颈在哪

脚本能做管线的每一环：建骨架、UV、烘贴图、导出、契约校验、按游戏灯光自渲预览。
**唯独「雕形」这一环是手的动作。**

这条界线是实测出来的，不是推测：摆图元 + 体素重构的路线迭代了十几轮
（耳朵四版、Skin 修改器五版），每次调好一处另一处就塌。程序化造型的上限
就是「还行的占位」。

所以分工必须切在这里：

> **造型由受过训练的人做（采购或委托），规格适配由脚本做。**

对应的落地路线见 `3d-asset-pipeline.mdc` 的「正式路线是采购网格 + 脚本适配」。

## 看竞品截图先判 2D 还是 3D

这一步跳过会让后面所有决策错位。

那几张作为目标的粉色系养成游戏截图，宠物全是**2D 插画 + 骨骼动画**
（Spine 那类），不是 3D 模型。它们的观感来自**插画功底和 UI 工艺**，
和模型精度无关。

后果有两个：

- 要那个观感，该找的是**插画师**，不是 3D 建模师。两个岗位技能几乎不重叠
- 别拿它当 3D 模型的验收标准。同样的钱，2D 在这个品类里出片更好看，
  但会失去换装 / 皮肤 / 动画复用的成本优势（见 docs/06 §5.1）

另外**参考图上的白纱要认出来**：带手指提示和广告位的那种发灰发白的截图，
是新手引导的遮罩，不是它的原始配色。照着调色会调歪。

## 模型不在引擎商店，这是结构性的

Cocos 商店搜「猫」出来的全是成品游戏和模板，不是模型。两个原因：

1. **上架需要营业执照**（免费资源也要），把卖模型的主力——个人 3D 美术——
   直接挡在门外了
2. **美术资源天生跨引擎，代码不跨。** 一个 FBX 在 Unity / UE / Cocos 都能用，
   美术只会去最大的市场上架；插件和成品项目绑引擎，只能在 Cocos 卖

所以引擎商店卖「绑引擎的东西」，通用素材市场卖「不绑引擎的东西」。
**模型属于后者，别在 Cocos 商店找。**

「原生格式，导进来不用转」这个理由也是虚的——**glTF 本来就是通用格式**，
Cocos 直接吃，从哪下的 `.glb` 都一样。

## 渠道（按先试哪个排序）

| 站 | 授权 | 备注 |
| --- | --- | --- |
| **Quaternius** quaternius.com | **全部 CC0** | 低模游戏素材，有成套动物包。风格是硬边低多边形 |
| **Poly Pizza** poly.pizza | CC0 / CC-BY | 继承 Google Poly 的库，低模动物多，直接下 glb |
| Sketchfab | CC0 / CC-BY 居多 | 量最大。**付费商店 2024-10 已关**，付费搬到 Fab |
| Fab fab.com | 逐个看 | Epic 统一市场，继承了 Sketchfab Store + UE 市场 |
| CGTrader | royalty-free | game-ready 低模多，性价比好 |
| Unity 资源商店 | 标准 EULA | **可以用在 Cocos**（Unity 官方支持文档明确说了）。但下载必须经过 Unity 编辑器，而且标 **Restricted Asset** 的有额外限制 |
| 爱给网等国内站 | **常常写不清** | 本项目要接流量主广告属商业用途，授权不明的不能用 |

## 授权红线

- **必须「允许商用 + 允许修改」**。修改这条容易漏——我们一定会重拓扑、重绑骨架
- **CC-BY 要署名**。落到小游戏上就是要在设置页或关于页留一块「素材来源」，
  列作者名和链接。不麻烦，但**上线前才发现要加一个页面就很被动**，提前规划
- 不想署名就挑 CC0，或者买付费的（通常是私有授权）

## 挑选标准

| 项 | 要求 | 为什么 |
| --- | --- | --- |
| 格式 | FBX 或 glTF/GLB | Cocos 直接吃 |
| 面数 | 三千以内最理想 | 超太多重拓扑成本高 |
| 骨架 | **不需要**，静态的就行 | 反正要重绑到我们那 28 根，带骨架反而要先删掉；不带绑定的通常便宜不少、可选范围也大得多 |
| 比例 | 和已采用的模型同一档 | 混用比例就没法共用骨架，动画和配饰翻倍 |
| 贴图 | 能拿到源文件 | 只有烘死在模型里的没法改 |

**别自己肉眼判断合不合规**，丢给 `inspect_source.py` 和 `inspect_fbx.py`，
几秒出面数、拓扑、UV 重叠、贴图规格、以及适配工作量。

## 采购之后一定会缺的东西

任何买来的模型都有它自己的骨架和命名，所以这几项**必然**要重做：

- 我们的动画绑在那 28 根骨骼名上，买来的用不了
- `socket_hat` / `socket_neck` 不存在，配饰挂不上
- `part_ears` / `part_neck` 不是独立网格，遮挡规则失效
- 面数、贴图尺寸大概率超标

这些都是脚本能做的（`build_pet_from_source.py`），但要**先知道它们一定要做**，
不要以为买回来就能用。

## 换网格时骨架坐标是可以重拟合的

契约里**骨骼名、层级、`socket_*` 是死的，28 个坐标是活的**。

这条是整条采购路线成立的前提：`animations.py` 按骨骼名生成曲线，
坐标改了重跑一遍就有新动画，不用重做动画。所以换一具网格的代价是可控的。

但要记住连带影响：**动画的幅度和旋转轴是按旧比例调的**。
比如尾巴从「朝后下方」变成「竖起来」，摆尾的旋转轴就完全不同了，
坐标改完还得回去看动画。

## 委托定制时交什么

一次性给对方三样，缺一样都会返工：

1. `assets-src/concept/` 里的三视图（同比例、同基准线）
2. `docs/06` 第五节的完整规格
3. **一句必须强调的话**：所有物种共用同一套 28 根骨架，
   耳朵和颈部要独立网格

验收跑 `inspect_fbx.py`，不合规当场报出是哪一条。
**这个机器可校验的契约是把「外包」从赌博变成例行工作的关键**，
大部分小团队没有，值得用起来。
