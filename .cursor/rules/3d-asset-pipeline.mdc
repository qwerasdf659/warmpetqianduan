---
description: 3D 资源管线（Blender → glTF → Cocos）的工具、契约与陷阱
globs: tools/**,assets-src/**,assets/scripts/ui/PetStage.ts,assets/scripts/ui/petSockets.ts
alwaysApply: false
---

# 3D 资源管线

> **⚠️ 已退役（2026-08-30）：项目从 3D 改为 2D Spine 骨骼。** 本管线（Blender →
> glTF → Cocos）连同 `tools/` 下的 Blender 脚本、`assets-src/pet/*.glb`、
> `assets/resources/models/*.glb`、`petSockets.ts` / `SocketProbe.ts` 一并停用，
> **先不删、仅停止往前推**。新方案见 `docs/06-UI设计规范.md` §3.4 / §5（2D Spine
> 双骨架：拟人主宠 + 四足小宠，runtime 用 Spine，资源放 `assets/resources/spine/`）。
> 以下内容仅供历史参考。

**管线是脚本化的，不是手工的。** 模型、骨架、动画、配饰全部由 `tools/` 下的
Blender 脚本生成，可复现、可进 git、可自动校验。手工在 Blender 界面里调完导出，
下次就重现不出来了。

## 先跑这两条

```
tools\doctor.ps1     # 环境齐不齐（换机器、升级 Blender 之后先跑）
tools\verify.ps1     # 全量验收：环境 + 产物一致性 + 三个模型的契约 + 客户端类型
```

`verify.ps1` 十几秒跑完，改完模型或贴图直接跑它，别一条条手敲。

## 环境是钉住的，而且跨两个 Python

版本要求只写在 **`tools/toolchain.json`** 一处，脚本和这份文档都不再重复写。
当前锁定 Blender **4.5 系列**（实测 4.5.10 LTS）。跨系列升级 glTF 导出器和
bmesh 的行为会变，产物不会报错、只会和契约对不上——真要升就先跑一遍 `verify.ps1`。

最容易踩的是**管线跨两个互不相干的解释器**：

| 解释器 | 谁在用 | 依赖 |
| --- | --- | --- |
| Blender 自带 Python 3.11 | 10 个 `import bpy` 的脚本 | numpy（Blender 自带） |
| 系统 Python 3.13 | `make_2d_assets.py`、`sync_resources.py` | Pillow，见 `tools/requirements.txt` |

**往系统 Python 里 `pip install` 不会让 Blender 看见，反过来也一样。**
症状是 `ModuleNotFoundError`，但你明明刚装过。`doctor.ps1` 会分别报两边缺什么。

Blender 装在别处：`$env:WARMPET_BLENDER = 'D:\...\blender.exe'`。

| 脚本 | 职责 |
| --- | --- |
| `rig_spec.py` | **骨架契约**：28 根骨骼的名字、层级、坐标、动画清单、各类预算。唯一事实来源 |
| `inspect_source.py` | **量外来网格**：归一化到高 2.0 / 朝 +Z，打三视图和关节关键点。**收 FBX 和 glTF** |
| `build_pet_from_source.py` | **把外来网格适配成合规资产**（减面、切耳、套骨架、蒙皮、简化贴图、导出） |
| `build_skeleton.py` | 程序化生成造型 + 骨架/蒙皮/导出的公共实现。**现在出包的猫是 Tripo 图生 3D 的，走 build_pet_from_source** |
| `species.py` | **猫狗各自的剖面规格**（形体、耳型、尾型、分区毛色），骨架不在这里 |
| `animations.py` | 动画曲线，`--anim-gain` 可放大幅度用于调试 |
| `pet_texture.py` | 分区毛色 + 毛流 + 眼睛贴花，烘焙成 BaseColor / Normal 两张 512² |
| `make_fur_detail.py` | 把毛发照片压成可平铺的细节图，**跑一次就行**，产物进 git |
| `make_eye_texture.py` | 程序化画眼睛贴花（眼廓 / 虹膜 / 瞳孔 / 星形高光），同上 |
| `build_accessory.py` | 配饰，刚体挂载 |
| `inspect_fbx.py` | **契约校验**，任意 FBX/GLB 都能查，不合规返回退出码 1 |
| `render_preview.py` | 按游戏相机/灯光/**着色方式**渲染预览图，默认卡通 |

**所有 Blender 脚本都通过 `tools\blender.ps1` 调**，它负责解析 blender.exe、
断言版本、补上那串无头开关（`--background --factory-startup -noaudio
--python-exit-code 1`）。这些开关一个都不能漏：少 `--factory-startup`，
本机的偏好设置和插件会污染产物；少 `--python-exit-code 1`，脚本抛异常了
Blender 照样返回 0。

**每个物种都要各建一次**，`--species` 决定用哪份剖面规格：

```
tools\blender.ps1 build_skeleton.py --out assets-src/pet --species dog
tools\blender.ps1 build_skeleton.py --out assets-src/pet --species cat
```

负值参数照旧要写等号形式（`"--forward=-y"`），wrapper 是原样转发的。

漏建一个物种不会报错，只会让 `PetStage` 静默退回狗——玩家养的猫长着狗的样子。
`SPECIES_MODEL` 里有几个物种，`assets/resources/models/` 就要有几个 GLB。

改完模型或动画**必须跑校验**。`--kind` 决定按哪一类资源的预算判，默认宠物：

```
tools\blender.ps1 inspect_fbx.py --file assets-src/pet/pet_dog.glb
tools\blender.ps1 inspect_fbx.py --file assets-src/accessory/acc_cap.glb --kind accessory
```

不传 `--kind` 去查配饰会报两条假失败（配饰没有也不需要 `part_ears` / `part_neck`）。

外部来的模型（美术交付、Meshy 产出）也用同一个脚本验收，不合规当场报出是哪一条。

## 产物的单一事实来源是 `assets-src/BUILD.json`

这条链的最后一段——从 `assets-src/pet/*.glb` 复制到 `assets/resources/models/`
——以前是手工做的，既没记录也没校验。结果同一个 `pet_cat.glb` 在工作区里
存在过六份，大小各不相同，没人说得清哪份在出包。

现在这一段是一条命令：

```
python tools\sync_resources.py            # 同步 + 更新清单
python tools\sync_resources.py --check    # 只校验，不一致就退出码 1（verify.ps1 里的第 2 关）
```

清单里每个出包模型记三样：**sha256、由哪个源产物来、以及重建它的确切命令**。
「哪份是当前正确的产物」的答案就是清单里那个 sha256，别再靠文件大小猜。

**加物种时要同时改 `sync_resources.RECIPES`**，否则新模型不会被同步，
`PetStage` 会静默退回狗。

### 实验产物写 `.build/`，不要写 `temp/`

`temp/` 是 Cocos 自己的目录，而 `cocos-toolchain` 规则里「预览坏掉就删
`temp/` `library/` `build/`」的恢复流程**会把你的中间产物一起删掉**。
之前四批实验产物就堆在 `temp/chibi`、`temp/eye`、`temp/skintest`、`temp/dryrun` 下，
一直活在被顺手删除的风险里。

`.build/` 已经 gitignore，早期那几批实验产物移到了 `.build/attic/`。

### 外来网格要记来源

`assets-src/pet/source/SOURCES.md` 记每一具采购网格的出处、授权、署名文本。
模型文件本身不带授权信息，下载时知道、三个月后就只剩一个文件名——
`cat_source.glb` 这个名字里已经看不出它来自 Poly 了。CC-BY 的署名页要在上线前就位。

## probe 脚本分两类，别当成一次性的删掉

`_research/probe-*` 里有四个是**管线的定标仪器**，`tools/` 的脚本注释直接引用它们，
阈值改了要用它们重新量：

| 脚本 | 量什么 | 谁引用 |
| --- | --- | --- |
| `probe-ear-split.py` | 耳朵切割面的实际接缝高度 | `build_pet_from_source.py` |
| `probe-eye-uv.py` | 眼球 UV 岛的像素尺寸 | `build_skeleton.py` |
| `probe-muzzle.py` | 吻部融合后的实际前表面 z | `species.py`（两处） |
| `probe-skin-cracks.py` | 重合顶点对逐帧的分离距离 | 本文档「重合顶点的权重必须统一」 |

另外三个是引擎侧的回归检查，加图标、改 `pet_toon.mtl` 时要重跑：
`probe-toon-effect.js`（取 builtin-toon 的 uuid）、`probe-toon-material.js`
（验 5 个 pass 和宏名）、`probe-icons.js`（验图标导成了 sprite-frame）。

只有 `probe-outline.py` 是真的一次性诊断，结论已经收进本文档的「反向壳描边」一节。

---

## 正式路线是「采购网格 + 脚本适配」，不是脚本生成造型

脚本能做管线的每一环（建骨架、UV、烘贴图、导出、校验），**唯独「雕形」这一环
是手的动作**。摆图元 + 体素重构的上限就是「还行的占位」，这一点迭代了十几轮
反复验证过：耳朵四版、Skin 修改器五版，每次调好一处另一处就塌。

所以分工切在这里：**造型由受过训练的人做（采购或委托），规格适配由脚本做。**

```
下载/采购的 GLB
  → inspect_source.py   量尺寸、定朝向、归一化、打三视图和关键点
  → 把关键点填进 rig_spec.BONES（28 个坐标是**活的**，跟着网格拟合）
  → build_pet_from_source.py   切耳朵 + 套骨架 + 蒙皮 + 生成动画 + 导出
  → inspect_fbx.py      验收
```

**契约里骨骼名、层级、`socket_*` 是死的，28 个坐标是活的。** 这条是整条路线
成立的前提：`animations.py` 按骨骼名生成曲线，坐标改了重跑一遍就有新动画，
不用重做——所以换一具网格的代价是可控的。

`build_skeleton.py` 那套程序化造型（`species.py` 的图元规格、`--body blob/skin`）
只在 `rig_spec` 还是 Q 版坐标时成立。坐标一旦拟合到外来网格，两者的比例就不匹配，
那条路生成的东西是错的——脚本会打警告，别忽略它。

## 拟合自某一具网格的绝对阈值，换比例一定会失效

管线里散着一批常数，是当初照某一具网格量出来的。**换比例时它们不会报错，
只会静默给出错的结果。** 已经踩过的三个：

| 常数 | 原值的来处 | 换到 Q 版之后 |
| --- | --- | --- |
| `inspect_source.GROUND_BAND`（原 `y < 0.16`） | 写实猫腿长 0.9 | Q 版腿全长 0.34，一下吃掉半条腿，四爪 z 被抹平 |
| `inspect_source` 的尾巴判据（原 `y > 1.35`） | 写实猫只有竖起的尾巴超过 1.35 | Q 版头顶 1.72、耳尖 2.00，**把整个头当尾巴量**（1872 个顶点） |
| `build_pet_from_source.EAR_MIN_Y/Z`（原 1.19 / 0.70） | 写实猫头顶 1.36、头部 z 到 1.18 | Q 版最大 z 只有 0.60，**一个面都选不出来** |

所以这类界一律从 `rig_spec` 的骨骼坐标推导，不写字面值。

**但骨骼起点不等于网格接缝。** `ear_L` 的 head 埋在颅骨里面，可见的耳朵网格
底边比它低一截——按耳骨坐标减 0.02 只切下 173 面（真值 340），要减到 0.12
才切得干净。这个数必须**按网格定标**，用 `_research/probe-ear-split.py` 扫，
不要凭骨骼坐标推出来就用。

定标时宁可多切也不要少切：多切的是颅骨顶，藏耳时那个洞永远在帽子底下；
少切留下的耳朵碎片是常驻可见的一块异色。

## 构建脚本不许在缺件时报成功

上面那些阈值失效之后，`build_pet_from_source.py` 打的是
`WARNING: no faces matched` 然后**继续走到 `BUILD_OK`、退出码 0**。
产物少了 `part_ears`，要到 `inspect_fbx.py` 才被拦下——而中间那段时间里，
它看起来完全是「构建成功」的。

`inspect_source.py` 是同一个毛病：Icosphere 混进测量、缩放系数算成 0.667
（正确值 0.978）、四爪坐标左右不对称，全程零报错，最后照样打 `SOURCE_OK`。

**契约要求的部件缺了就 `raise SystemExit`**，别指望下游那道验收。
下游能兜住产物，兜不住你已经按「构建成功」往下推的那几步判断。

## 外来网格：UV 和贴图必须原样保留

低多边形模型的 UV 指向一张很小的色块调色板（那只猫是 **32×32**），
**重新 `smart_project` 会把顶点指到错误的色块上，颜色全毁。**

所以适配外来网格时不要 `pack_uv`、不要烘贴图。我们自己那套「分区毛色 + 毛流烘焙」
是为程序化造型准备的，对采购的模型不适用。

## 图生 3D（Tripo / Meshy）的接入流程

出包那只 Q 版猫就是 Tripo 图生 3D 出来的，方向 A 的元素（大眼星形高光、腮红、
铃铛、星星肚兜）自带得比程序化那只齐。整条链跑通了，几个要点：

**导出选 GLB，不要 FBX。** 理由和我们自己导出一样（FBX 厘米单位 → 100× 缩放节点）。
Tripo 那步的「绑骨」是白花积分——我们进管线第一步就把它的骨架整个剥掉，
套自己那 28 根。它只需要给「网格 + 贴图」。

**朝向要人眼定，自动判会错。** `inspect_source` 的 `detect_long_axis` 对
**站立**的 Q 版猫失效——它假设身长是水平最长轴，而站姿猫的最长轴是竖直的高度。
Tripo 猫自动判成 `forward=-y`（侧对镜头），实际要 `--forward=-x`。
判据只有一个：先不传 `--out` 渲一张，脸没对着镜头就换一个 forward 重试。
四爪的「左右不镜像」警告也是同一个信号——朝向错了，左右腿就分不对。

**千万别减面。** 这是栽过的坑：一开始按「宠物预算 3000」把 20700 减到 2910，
结果**脸上全是撕裂的白色条纹**。原因是 Tripo 的贴图是**碎片化 UV 图集**
（几十个小岛、彼此没留白），COLLAPSE 减面会把 UV 缝上的顶点也合并，
三角形跨到不同色块之间、把别处的颜色拉过来。实测 8000 面都还在撕。
所以 `TRI_BUDGET["pet"]` 已抬到 22000：宠物在屏上只有 1–2 只，
20700 面单只骨骼角色蒙皮约 11000 顶点、DrawCall 1–2，低端机毫无压力
（瓶颈从来是 DrawCall 和过度绘制，不是这个量级的顶点）。
`build_pet_from_source` 仍有 `decimate_to_budget`，但对这类模型是空操作。

> 3000 那个数是给「低端机 + 同屏多角色 + 自烘低频贴图」的组合定的，
> 三个前提对图生 3D 的英雄宠物一个都不成立。别拿它去卡。

**`--no-ears` 整只不切。** 又一个和 `part_neck` 同类的坑：从连续网格上把耳朵
切出来单独蒙皮，那片薄薄的**内耳膜**会被扯成一片耷拉的粉色碎片
（切颈环露洞、切耳膜扯烂，都是「连续高模不能按部件切」的同一条）。
所以图生 3D 的宠物用 `--no-ears`，`inspect_fbx` 会为缺 part_ears / part_neck
报两条 FAIL，**都是已知且接受的**。代价是丢掉「帽子遮耳」（§5.5），可接受。

**贴图只留 BaseColor、提到 1024**（`simplify_material`，`TEX_SIZE["pet"]=1024`）。
Tripo 给一个材质挂三张图：BaseColor 8192²、法线 4096²、ORM 4096²。
游戏里宠物走 `builtin-toon`，只采样 mainTexture(BaseColor)，法线和 ORM 用不上。
为什么 1024 而不是 512：图集碎、脸只占一小块，512 下脸放大到屏幕上发软
（现象：**UI 文字锐、独独宠物糊**）。1024 显存 4MB，够且不奢侈。
这不违反「UV 原样保留」：只换分辨率，不重新展 UV、不重烘。

**碎图集必须在运行时关 mipmap**（`PetStage.disableMipmaps` → `setMipFilter(NONE)`）。
默认开 mipmap 会把没留白的相邻小岛逐级平均到一起，糊成水彩——比分辨率更致命，
而且自渲预览看不到（Blender 走全分辨率单张采样）。宠物相机基本不动，不需要 mipmap。

**站姿和我们 arms-down 的骨架不完全匹配，但 idle 扛得住。** Tripo 猫前爪
举在两侧，而 rig_spec 的前腿静止在贴地位置。自动权重会打一句
`failed to find solution for one or more bones`，`fill_unweighted` 兜底。
idle 只有呼吸和摇头、几乎不动四肢，所以举起的前爪不会被扯裂——渲 idle 中间帧
验证过。要做挥手这类动前肢的动画，才需要回去把前腿骨重新拟合到举起的位置。

> **一句话总结这次的教训**：为程序化占位造型和低模 Poly 猫设计的那套
> （减面到 3000、切耳切颈、烘 512 贴图），套到图生 3D 的高质量连续网格上
> 会**逐条把它毁掉**。图生 3D 的正确姿势是「基本不动几何、只做归一化 + 套骨架 +
> 蒙皮 + 换贴图分辨率 + 关 mipmap」。命令：
> `build_pet_from_source ... --no-ears --tri-budget 40000`（贴图默认已 1024）。

## `part_neck` 在连续网格上做不到，这是刻意的取舍

§5.5 要求颈部能单独隐藏（戴围巾时），但那条规则假设按部件建模的网格。
从一具连续网格上切下一圈颈环，**隐藏它会露出一个洞**，比围巾穿模更糟。

所以外来网格只切耳朵（戴帽藏耳是真正常见的冲突，而且耳朵是天然可分离的凸起），
围巾改用「做大到能包住颈部」来避免穿模。`inspect_fbx.py` 会为此报一条 FAIL，
那是**已知且接受**的偏差，不要为了让它变绿而去切颈环。

## 编辑模式的选区不能在物体模式里设

```python
for poly in mesh.polygons:   # 不管用
    poly.select = ...
```

进编辑模式时 Blender 按当前选择模式重新推导选区，而**刚导入的网格顶点往往是
全选状态**——结果 `mesh.separate` 把整只猫都划给了耳朵。

更坏的是报错位置离原因很远：`part_body` 变成空网格，
到导出阶段才报 `"has no primitives"`，再到 `min() arg is an empty sequence`。

正确做法是进编辑模式后先 `select_all(action="DESELECT")`，
再切 `select_mode(type="FACE")`，然后用 bmesh 逐面设 `face.select`。

## 重合顶点的权重必须统一，否则网格会裂开

低多边形模型在硬边和 UV 缝处顶点是**成对重复**的——那只猫 1120 个顶点里
821 个是冗余的（594 面却有 1120 顶点，比例明显不对就是信号）。

这类网格有一种特有的失效方式：**一对本来重合的顶点被指派给了不同骨骼，
骨骼一动它们就分开，网格上裂出缝。** 玩家看到的是「模型被切成一块一块的」。

最坏的地方在于**静止姿态完全正常**，所以在 Blender 里摆着看、渲静帧都发现不了；
而且它是渐变的，小裂缝在预览图上看不出，到游戏里镜头一近就明显。

成因是权重来自两个不同来源：自动权重覆盖到的顶点走热力图，没覆盖到的走
`fill_unweighted` 的硬指派。一对重合顶点各走一条，结果就不一致。

修法不是去改某一个来源，而是最后统一抹平：按位置分桶、桶内取平均权重再归一化
（`build_skeleton.unify_coincident_weights`）。只要重合顶点权重相同，
无论怎么运动都不会分开。合并会取骨骼并集，可能超过 4 根，所以要先截断再归一化。

**验证要用数据，不能靠看。** `_research/probe-skin-cracks.py` 找出静止时重合的
顶点对，逐帧算它们的距离：静止为 0、动起来还是 0 才算没裂。
修之前最大裂口 0.054 单位（集中在颈肩接缝和尾根），修完全帧 0.0000。

## 自动权重可能一个顶点组都不建

`ARMATURE_AUTO` 不保证给每个部件都建出顶点组，外来网格上尤其常见
（切下来的小块耳朵）。而 `vertex_group_limit_total` 在没有顶点组时**直接抛异常**，
不是静默跳过。

所以调用前要判 `if obj.vertex_groups:`，真正的兜底是 `fill_unweighted`
按距离把顶点指派给最近的骨头。

## 导出用 glTF，不要 FBX

**FBX 的长度单位是厘米**，导进 Cocos 后根节点会带一层 100× 缩放来补偿。
这和「按成长阶段缩放宠物根节点」（docs/06 §5.7）直接冲突——
前端写 `node.scale = 0.72` 会把宠物缩到 1/100 大。

glTF 原生用米，没有补偿节点。而且同一模型 GLB 比 FBX 小约 70%。

## `add_leaf_bones` 默认是 True

FBX 导出时它会给每条骨链尾端追加一根 `*_end`，28 根骨架变成 40 多根，
直接突破 30 根上限。必须显式设 `add_leaf_bones=False`。

同类：`use_armature_deform_only=True` 会把不蒙皮的 `socket_*` 挂点过滤掉，
换装挂点就没了。

## 体素重构会抹掉 UV

`Remesh` 重新生成表面，UV 不会保留。没有 UV 的网格根本没法贴图。

重构后要补一次 `smart_project`。**校验规则要写「恰好一套 UV」而不是「不超过一套」**——
后者在一套都没有时会假性通过，这个假阴性我们踩过。

## 自动权重漏掉的顶点会凭空长出一根骨头

glTF 导出器一旦发现有顶点没有任何权重，就会插一根 `neutral_bone` 兜底，
骨骼数 +1，违反契约。

自动权重（`ARMATURE_AUTO`）不保证覆盖所有顶点，尤其是离骨骼较远的部位。
导出前要把无权重顶点按距离显式指派给最近的骨头，见 `build_skeleton.fill_unweighted`。

另外每顶点最多 4 根骨骼影响（shader 的 `a_weights` 是 vec4），
超出的会被引擎静默丢弃，所以要在导出前 `vertex_group_limit_total(limit=4)` 再归一化。

## glTF 存的是秒，不是帧

关键帧时间以秒记录，**导入时按当前场景帧率重新采样**。
不先把场景 fps 对齐到 30 就去量帧率，量到的是导入方的帧率，和创作时无关。

`inspect_fbx.py` 在导入前会设 `scene.render.fps`，改那里要一并考虑。

## 循环动画的频率必须是周期的整数倍

用正弦驱动循环动画时，半周期（`sin(phase * 0.5)`）会让首尾帧对不上，
每圈跳一下。要错开节奏就加相位偏移（`sin(phase + 2.1)`），不要改频率。

## Blender 的 glTF 导入器会造辅助网格

导入带骨骼的 glTF 时会生成骨骼显示用的 `Icosphere`。
它不属于模型资产，统计面数和部件时必须滤掉，
判据是「是否被某个 pose bone 用作 `custom_shape`」。

## `img.save()` 用相对路径会静默不落盘

Blender 把相对路径按「相对于 .blend 文件」解析，而无头脚本里没有 .blend。
`save()` **不报错**，文件却不知道去了哪。

症状极具误导性：GLB 体积正常增长（导出器发现文件读不到，退回用内存里的像素），
磁盘上一张贴图都没有，看起来像是「贴图进了 GLB，只是没导出副本」。

所以 `pet_texture._save` 一律 `os.path.abspath` 再存，存完 `os.path.exists` 断言一次。

## 顶点色只有接进着色器的那个材质球才烘得出来

`paint_vertex_colors` 把分区毛色和五官颜色写成顶点色，但**烘焙读的是材质的
着色器**，不是顶点色本身。所以每一个参与烘焙的材质球都要自己接一次顶点色。

我们在这上面丢了很久：`pet_detail`（眼睛、鼻子、嘴）一直是个纯色材质球，
只有 `pet_body` 建了烘焙着色器。结果虹膜、瞳孔、鼻子分别算好的颜色一个都没进贴图，
全身五官烘出来是同一片死板的深色。

**症状是「眼睛读成两个洞」，而它看起来完全像个造型问题** ——
所以前面几轮一直在改眼球几何（加瞳孔圆盘、加虹膜盘、加高光小球），
每一版都更复杂、都不管用。这类「颜色不对但几何没错」的问题，
先去确认那个材质球的着色器里到底有没有顶点色节点。

## 部件面数 = 融合预算 + 五官面数

五官（`pet_detail` 的那些球和锥台）是**融合之后**才 join 进 `part_body` 的，
所以 `species.py` 里 `fuse["part_body"]` 那个数不是部件的最终面数。

猫的实测：融合预算 1700 + 五官 748 = 2448。光看着「1900 < 3000」就改，
出来是 3168，`inspect_fbx` 直接 FAIL。改预算前先把五官那部分算进去。

顺带一条尺度判断：眼球默认那档十段六环的球，在脸上读成一个**八边形**。
眼睛是全脸唯一被反复注视的地方，值得单独提到 16×10（每只 +176 面），
代价从身体的融合预算里出——融合出来的身体是个光滑的团，300 面看不出来。

## 贴图发虚先量 UV 岛，不要去改贴花

星形高光在游戏里边缘发虚，第一反应是「贴花画得不够细」——错的。
`smart_project` 按角度切缝，把**每只眼球切成 6 个 35×36 px 的小岛**
（`_research/probe-eye-uv.py` 量的），图案跨在六块之间，每块四周还各有 6 px 留边。
贴花本身是 256²，落地只有 35 px，再怎么画都是虚的。

一个容易骗过自己的现象：烘出来的图集上能数出**七八颗完整的星星**，
而眼球只有两颗。那不是贴花漏到别的几何上了，是同一颗星被切在不同岛里、
每块都带着一部分，缩略图上看着像各自完整。**别靠看图集缩略图判断，去量。**

修法是给眼球单独留一块大岛（`build_skeleton.project_eye_uvs`）：
图集整体缩到 `[0, BODY_UV_FIT]²`，空出来的边给眼睛，UV 直接按贴花那套
光轴平面投影算，纹素网格和贴花像素对齐。

**只搬朝前那半个球。** 整个球搬过去，前后两半会投影到同一块 UV：
烘焙时互相覆盖，而且 `inspect_fbx` 的 UV 重叠检查直接 FAIL。

一条更一般的推论：**我们的贴图内容是在三维空间里定义、再烘到 UV 的，
所以 UV 拉伸不会让图案变形，只改变局部分辨率。** 因此可以为了「把像素花在
该花的地方」而任意重排 UV——这个自由度在手绘贴图的流程里是没有的。

## 球面上的平涂要靠拍平法线，不是靠改贴图

方向 A 的眼睛是 2D 平涂，而球面在三档卡通着色下必然有明暗渐变：
朝侧面那一圈被乘上 `shadeColor`，压出一块阴影，眼睛读成玻璃弹珠。

在贴图里反向补一层亮度是错的（那是把光照画进 BaseColor，违反 §5.8）。
正确做法是把那块面的法线**全部改成同一个方向**，让 `0.5*dot(N,L)+0.5`
在整片上取同一个值。剪影不变，面数不变。见 `build_skeleton.flatten_eye_normals`。

两条连带影响：

- 拍平要放在**烘焙之后**。法线贴图那一遍烘的是着色法线，先拍平会把那块烘成一片平面法线。
- 引擎的描边 pass 是 `localPos += normal * lineWidth * 0.001`，
  拍平之后那块的描边壳变成「整体沿法线方向平移」而不是「向外扩」。
  对眼球正合意（眼廓已经画在贴花里），但换到别的部位要先想清楚。
- **预览里的描边壳也必须读逐角法线**（`mesh.corner_normals`），不能用 `vert.normal`。
  引擎读的是 glTF 的 NORMAL 属性，带自定义法线的网格导出时按属性拆点；
  `vert.normal` 是几何平均值，用它预览会画出引擎里并不存在的描边。

## 五官贴在哪个 z 上，只能量，不能估

`species.py` 那些吻部、鼻子、嘴的坐标必须落在**融合之后的实际表面**上。
体素重构 + 平滑会把表面往里收，收多少算不出来；而头部造型一改，吻部整个挪位置。

估错的症状很有欺骗性：不是「五官不见了」，而是**露出一堆碎片**，
读起来像一团污渍或一撮胡子，看不出是「埋进去了」。

量法见 `_research/probe-muzzle.py`：导入建好的 GLB，按 Cocos y 分层报告最前表面。
猫的实测是吻部前表面在 z=0.714，而上一版鼻子中心 0.680、半深 0.037，
**只凸出 0.003 个单位**——屏幕上不到半个像素。

推论：吻部又小又浅的 Q 版脸上，**半埋的锥台做不出嘴**。露出来只有 1–2 个像素，
怎么摆都是一条碎线。嘴要么做成贴花（和眼睛同一套机制），要么不做。

## 烘焙的三个坑

**`use_clear = True` 会把 UV 壳之外刷成纯黑。** 压缩和 mipmap 会把那圈黑渗进壳内，
模型上出现一条条暗缝。改成 `use_clear = False`，靠 `image.generated_color`
预先填底色（法线图填 `(0.5, 0.5, 1)`）。

**样本数不能给 1。** 颜色和法线都不需要光照收敛，但**需要抗锯齿**：
每纹素只采一个点时，比纹素更细的纹理内容会留成一片随机麻点。给 16。

**顶点色导出前要删掉。** 顶点色只是烘焙的输入，留着会被 glTF 导出器
当成 `COLOR_0` 带进文件；引擎一旦启用顶点色就会和贴图相乘，颜色平白暗一层。

## 共用材质的部件必须一起展开 UV

`part_body` / `part_ears` / `part_neck` 共用一个材质球，也就共用一张贴图。
各自 `smart_project` 的话三份 UV 全都铺满 0–1，在贴图空间里完全重叠，
烘焙时互相覆盖。

做法是多物体同时进编辑模式再 `smart_project`，Blender 会跨物体统一装箱。
`inspect_fbx.py` 有一条栅格化的重叠检查专门守这个（顺带也守
「不允许镜像重叠」——Blender 图元自带的 UV 就是重叠的，`acc_cap` 当年就中了）。

## 贴图分辨率决定毛的尺度，不是审美决定

512² 摊到整只宠物约 2 m² 表面，一个纹素折合现实约 3 mm。
比纹素细的结构存不进去：单采样变麻点，多采样变死板。

所以毛流的投影尺度是被分辨率**逼出来**的——细节图上横向约 200 根毛，
要让每根占 3 个纹素以上，一片就得铺到 2 米宽（`pet_texture.FUR_TILE = 0.5`）。
调这个数之前先算一遍纹素尺寸，不要凭手感。

同理，**程序化噪声做不出毛**。振幅 0.13 像砂纸，0.07 像塑料，中间没有档位——
方向性和成缕结构不是噪声的统计特征。用照片派生的细节图（`make_fur_detail.py`）。

## 法线贴图这里用 JPEG 是刻意的

一般来说法线贴图不该用有损压缩。这里例外：它只承载微观毛缕起伏，
512² PNG 是 478 KB（内容全是高频噪声，PNG 压不动），JPEG q94 是 1/5，
而 §4.4 的主包只剩不到 2 MB。两版渲染出来看不出差别。

将来法线贴图要承载缝线、鼻纹这类**结构性**细节时，要回头换成 PNG。

## `primitive_cube_add(size=1.0)` 的顶点在 ±0.5

所以 `obj.scale` 传的就是最终全长，不是半长。按半长算会得到一半大小的盒子。

---

## Skin 修改器那条路试过了，3000 面预算下打不过图元

`build_skeleton.py --body skin` 用 Skin 修改器 + 细分曲面从骨架图长出躯干四肢。
听起来明显更对：连续管面、分叉自动过渡、四边形网格有边流。**实测五轮，不行。**

| 配置 | 结果 |
| --- | --- |
| 细分一级 | 截面只有八边，躯干像倒角的木盒子 |
| 细分二级 | 圆了，减面前 9000 面 |
| 二级 + 减面到 2000 | 四条腿被合并成一个桶，腿间空隙全没了 |

根因：**Skin 每个环固定四条边，圆不圆只取决于细分级数**，加节点只加环不加边。
而二级细分是预算的三倍，减面又保不住相邻肢体之间的空隙。

另外两条附带教训：

- **分叉枢纽的形状不可控。** 头挂在图上出来比设定小一圈，眼睛耳朵全飘到头外面。
  头、耳朵这类形状吃紧的部位必须用图元单独做。
- **细分和平滑都会把表面往里收**，实际半径只有设定值的 0.75–0.85。
  按想要的尺寸填，出来是大头配一身细杆子。

代码保留在 `--body skin` 后面，默认是 `blob`。**什么时候值得回来用它：
面数预算抬到 6000 以上**（比如宠物按 §8.5 挪进远程 Asset Bundle 之后），
二级细分不用减面，上面三条问题一次全消。

## 剪影：耳朵压哪个轴、挂在哪里，比造型本身重要

相机固定在正前方（docs/06 §3.4），宠物在屏幕上只有两百多像素高。
后果是**只有耳朵能区分猫狗**——尾巴被身体完全挡住，体态差异只剩几个像素。

做耳朵栽过四次，每次的错法都不一样：

| 做法 | 结果 |
| --- | --- |
| 细圆管折一下 | 两段挨太近，被体素重构融成一根竖管，像兔子 |
| 压扁成带子走弧线 | **压错了轴**，正面只看到侧棱，像两根天线 |
| 立起来 + 尖端外折 | 两块方板扣在头两侧，像头盔 |
| 单块三角形但贴着头 | 整块埋在头的体积里，剪影上只剩两个疙瘩 |

两条可复用的结论：

**压扁要压 Z（前后），不是 X。** 耳廓是个面，得正对着固定的正面镜头。
压 X 那版几何上完全正确，只是镜头永远看不到那个面。

**耳朵必须挂到头的包围体之外。** 头的 x 半轴是 0.35，耳尖只到 0.305 的话
整块耳朵在头里面。加形体之前先算一下它露不露出来。

还有一条：**别为了凑总高改耳型。** 垂耳的最高点就是头顶，比尖立耳矮一档，
这是造型决定的。上面那三种失败形态全是为了把垂耳的狗凑到 2.0 单位。
§5.3 已经把总高放宽成 1.70–2.05 的区间，别再往回凑。

## 不要靠肉眼判断「动没动」

「看不出在动」有两种完全不同的原因：**动画没生效**，或者**动了但幅度太小**。
肉眼分不出来，修法却完全相反。

先用数据分清：
- `animations.py` 的 `--anim-gain` 把幅度放大若干倍，还是不动就与幅度无关
- 在运行时按时间多点采样骨骼的**局部**变换（不是世界坐标——
  父节点缩放会污染差值，我们就栽在把成长阶段缩放误读成动画位移上）

## 预览脚本必须锁 `Standard` 色彩变换，不能用 AgX

Blender 默认的 view transform 是 **AgX**，它把高光平滑地滚降回来；
Cocos 是**直接截断**。同一份过曝，在预览图里看不见，在引擎里是一片死白。

代价是实打实的：宠物毛色反射率 0.85，主光按全日照给了 88000 lux，
亮面在引擎里早就顶满了——毛流、反荫蔽、分区色全被削平，
而我照着 AgX 的预览一轮轮调造型，以为是模型不好看。

AgX 还会顺手降饱和，所以预览图偏灰，也会误导配色判断。

```python
scene.view_settings.view_transform = "Standard"
scene.view_settings.look = "None"
```

**更一般的教训**：自渲预览的价值取决于它和引擎的一致性。相机和灯光的
欧拉角、FOV 早就对齐了，唯独色彩管线没对齐，结果整条迭代链都偏了。
以后加任何新的渲染参数，先问一句「引擎那边是怎么处理的」。

## 能自己渲就不要让人截图

`render_preview.py` 用和 docs/06 §3.4 完全一致的相机、主光、环境光渲染。
改完模型直接渲一张看，迭代从分钟级压到秒级。

对应地，改了 §3.4 的相机或灯光参数，要同步改 `render_preview.py` 里的常量，
否则预览图和游戏里对不上，等于白渲。

### 预览必须按卡通着色渲，不能按 PBR

宠物的材质在运行时被 `PetStage.applyToonMaterial` **整个换成 `builtin-toon`**，
模型自带的 PBR 参数一个都不生效。所以 PBR 预览会在三件事上骗人：

- **高光**：`tuneToon` 把 specular 设成 0，而 PBR 预览里眼球和鼻头各有一块
  白色镜面反射。调星形高光的位置时，那块反射会混进来，根本分不清哪个是贴图画的。
- **法线贴图**：`pet_toon` 只开了 `USE_BASE_COLOR_MAP`，**法线贴图没有接口**。
  PBR 预览里那层木纹质感在游戏里不存在——照着它调毛流的凹凸强度是白费功夫。
  （附带结论：现在烘出来的那张法线贴图在游戏里没人用，只占包体。）
- **色阶和描边**：游戏里是三档平涂 + 反向壳描边，PBR 是连续渐变、没有描边。

所以 `render_preview.py` 不摆灯让 Blender 算光照，而是**照抄 builtin-toon 的算式**：
半 Lambert 的 NL 两次 smoothstep 分三档，乘 BaseColor 贴图，走 Emission。
参数逐个对齐 `PetStage.tuneToon`，改那边就要改这里。`--shading pbr` 能切回去看贴图本身。

之前连着两轮都在跟用户说「预览图是 PBR 渲的，不准，你去刷新看」——
那两轮的迭代其实都是在照着一个错的目标调。

### 反向壳描边：翻转法线必须在位移**之后**

描边壳的做法是「复制网格 → 沿法线外扩 → 翻转法线 → 开背面剔除」。
外扩要**直接改顶点**，不要用 Displace 修改器：修改器是渲染时才求值的，
翻转法线写在它前面，它就照着翻过的法线往里缩——壳比本体还小，描边一根都没有。

症状在包围盒上只是 0.03 的差，肉眼分辨不出来，只能量。

### glTF 导入器造的 `Icosphere` 不只是统计时要滤掉

已知它会污染面数统计。**更坏的一种用法**：它本体不在场景集合里所以不渲染，
但如果你给每个网格新建派生对象（比如描边壳）并 link 进集合，
就等于把一个 2 单位大的球的描边壳挂进了画面——表现为半只猫被一件深色斗篷盖住，
而这个现象离「描边」这个概念太远，完全想不到是同一段代码干的。

判据是「是否被某个 pose bone 用作 `custom_shape`」，见 `render_preview._bone_shape_meshes`。
