---
description: WarmPet 项目核心背景与铁律（每次必读）
alwaysApply: true
---

# WarmPet · 宠物养成 3D 微信小游戏

## 项目定位
- 玩法：养成为主 + 轻竞技为辅，按「偏静 / 热闹」二分承载多形态玩法。
- 变现：流量主广告（线上）+ 异业合作实物（线下），**全程不接入支付**。
- 与既有「天工」营销平台通过 UnionID 账号 + 统一积分账本打通。

## 五条铁律（任何代码/设计都必须遵守）
1. **服务端权威**：状态衰减、赛跑判定、奖励发放全在后端算，客户端结果不可信。
2. **不信任客户端时钟**：离线结算取 `min(客户端申报, now − lastSeenAtServer)`；时钟异常不作封号依据。
3. **软失败不死亡**：广告失败给保底、领奖幂等、弱网可重试，绝不因单次失败卡死玩家。
4. **积分双池隔离**：虚拟游戏币池 与 营销积分池 物理隔离，永不互转（合规红线）。
5. **积分只能向下兑权益，永不能换回现金**（预付卡监管红线）。

## 技术栈（详见 docs/08）
- 客户端：Cocos Creator 3.8.8 + TypeScript（引擎按 TS 4.1 语义编译）。
- 后端：NestJS 11 + TS 5.9 + PostgreSQL + Redis + TypeORM；REST 为主，实时用**原生 WebSocket**（不用 Socket.IO 客户端）。

## 工程现状
- 客户端是 **Cocos Creator 3.8.8 工程**（`assets/` + `settings/`），
  已不是原生小游戏工程，`js/`、`game.js`、`project.config.json` 都由构建产出。
- 网络层与全部 60+ 接口封装已完成，见 `assets/scripts/net/`。
- 后端服务在 `docs/后端配合需求清单.md` 记录的地址；不可用时用
  `node _research/mock-server.mjs` 起本地假后端，切换只改 `net/config.ts` 的 `DEV_BASE_URL`。

## 设计文档（`docs/`，只列实际存在的）
| 文档 | 内容 |
| --- | --- |
| `06-UI设计规范.md` | 设计 token、3D 模型/骨架/动画/贴图规范、美术交付与验收 |
| `玩法扩展设计.md` | P0–P13 十四项玩法的落地方案与接口契约 |
| `竞品玩法梳理.md` | Neopets / Adopt Me! / 模拟人生猫狗的玩法对照 |
| `后端配合需求清单.md` | 需要后端回复的问题、接口契约问题、故障证据 |

`前端对接文档.md`（仓库根目录）是后端提供的 API 契约，以它为准。

改动涉及上述主题时先对照对应文档；**文档与代码不一致时，两边一起改**。
