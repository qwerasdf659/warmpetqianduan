---
description: WarmPet 项目核心背景与铁律（每次必读）
alwaysApply: true
---

# WarmPet · 宠物养成 2D 微信小游戏

对外中文名 **暖宠小屋**（2026-09-07 定）。`warmpet` / `warmpet_*` 是内部代号与存储键，
**不随中文名改**——改了会让老玩家已存的 token 和冷却记录失效。

## 项目定位
- 玩法：养成为主 + 轻竞技为辅，按「偏静 / 热闹」二分承载多形态玩法。
- 变现：流量主广告（线上）+ 异业合作实物（线下），**全程不接入支付**。
- 与既有「天工」营销平台通过 UnionID 账号 + 统一积分账本打通。

## 五条铁律（任何代码/设计都必须遵守）
1. **服务端权威**：状态衰减、赛跑判定、奖励发放全在后端算，客户端结果不可信。
2. **不信任客户端时钟**：离线结算取 `min(客户端申报, now − lastSeenAtServer)`；时钟异常不作封号依据。
3. **软失败不死亡**：广告失败给保底、领奖幂等、弱网可重试，绝不因单次失败卡死玩家。
4. **积分双池隔离**：虚拟游戏币池 与 营销积分池 物理隔离，永不互转（合规红线）。
5. **积分只能向下兑权益，永不能换回现金**（预付卡监管红线）。

## 技术栈
- 客户端：Cocos Creator 3.8.8 + TypeScript（引擎按 TS 4.1 语义编译）。
- 后端：NestJS 11 + TS 5.9 + PostgreSQL + Redis + TypeORM；REST 为主，实时用**原生 WebSocket**（不用 Socket.IO 客户端）。

## 工程现状
- 客户端是 **Cocos Creator 3.8.8 工程**（`assets/` + `settings/`），
  已不是原生小游戏工程，`js/`、`game.js`、`project.config.json` 都由构建产出。
- 网络层与全部 60+ 接口封装已完成，见 `assets/scripts/net/`。
- **接口契约以 `net/types.ts` + `net/api/` 为准**（原后端提供的《前端对接文档》已不在仓库里）。
  两条贯穿全局的约定写在 `types.ts` 头部：bigint 主键在 JSON 里是字符串（不要 `Number()`）、
  时间是 ISO8601 UTC 而业务日切按东八区。
- 后端地址见 `net/config.ts` 的 `BASE_URL`；不可用时用 `node _research/mock-server.mjs`
  起本地假后端，切换只改同文件的 `DEV_BASE_URL`（上线前必须置空）。

## 玩法现状：接口很全，界面几乎没有（2026-09-07 盘点）

**别把「接口已封装」当成「功能已存在」。** 这是本项目最容易误判的一点：

- `net/api/` 有 13 个模块、60+ 接口，但 `ui/` 层只用到了 `pet`（四种互动）和 `wallet`。
  签到、任务、图鉴、扭蛋、赛跑、商店、消耗品、兑换、家园**一个入口都没有**。
- 于是**核心循环是断的**：玩家点四个按钮攒游戏币，界面上没有任何花币的地方。
  加任何新玩法之前，优先把已有接口接出入口——那是投入产出比最高的一步。
- `store` 的 `dailyBadge` / `dexBadge` 是**死代码**（红点计算写好了，从没被读过）。
  它们的存在说明当初规划过入口，不是遗漏。
- `ui/ShowcasePanel.ts` 是验证用的调试面板（换猫/换场景/换墙纸/顾客上门…），
  却是目前界面上内容最多的部分。**它把内容量提前透支掉了**，正式界面成型后要删掉。
- 其他缺口：全项目**零音效**（无 `AudioSource`/`AudioClip`）；宠物**没有起名入口**
  （主界面显示「还没起名字」，接口只在 `/pet/create` 时能传 `nickname`）。

## 设计文档（`docs/`，只列实际存在的）

| 文档 | 内容 |
| --- | --- |
| `06-UI设计规范.md` | 设计 token、界面/动效规范、Spine 骨架与动画规范、美术交付与验收 |
| `2D-Spine迁移与接入.md` | 3D→2D 的方向决策、单套四足骨架、资源约定与落地顺序 |
| `宠物Spine骨骼原理与外包说明.md` | Spine 骨骼原理、59 猫素材包盘点、外包报价与硬要求 |
| `小游戏包体与远程资源配置.md` | 主包/分包预算、远程资源与 CDN 配置 |

设计稿与规格表（`docs/*.png`）：首页定稿见 `first-screen-and-loading` 规则，
猫狗花色与耳型规格表见 `spine-2d-pet` 规则，命名与归档规范见 skill `design-mockup-archive`。

> `docs/cursor_*.md` 是历史会话记录，不是设计文档，别当规范引用。
>
> **已删除、只在 git 历史里的三份**：`玩法扩展设计.md`（P0–P13 十四项玩法方案与接口契约）、
> `竞品玩法梳理.md`、`后端配合需求清单.md`。需要时用
> `git log --diff-filter=D --name-only -- "docs/*.md"` 找到删除点再 `git show` 取出，
> **不要凭记忆复述它们的内容**。

改动涉及上述主题时先对照对应文档；**文档与代码不一致时，两边一起改**。
