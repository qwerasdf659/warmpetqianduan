---
description: 2D 宠物 Spine 的选型结论与接入踩坑（改 PetStage、导骨骼、换皮肤、加家具/挂点/拖拽、处理素材包前必读）
alwaysApply: true
---

# 2D 宠物 Spine · 选型与踩坑

宠物/家具动画一律用 **Spine 骨骼动画**（非帧序列）。骨骼+皮肤共用、贴图可换，
所以「一套骨骼 × N 种花色」成立，这是选它的核心原因。

下面每条都在本项目真实踩过，不是理论建议。美术侧的工具选型与绑骨底图要求见
`2d-skeletal-art-tooling`，出包/CDN 见 `cocos-toolchain`。

## 选型结论（已定，别再推翻）

- 宠物 = 2D Spine 骨骼，**不是 3D、不是序列帧**。3D 管线（Tripo、`tools/` Blender、
  `*.glb`）已退役，别再往前推；概念图留在 `assets-src/concept/`。
- **全项目只有一套骨架：四足，猫狗共用**（2026-09-07 定）。它同时是养成主角、
  收集品、环境角色和赛跑角色。**「拟人主宠」方案已废弃**，别再按双骨架规划
  （理由见 `docs/2D-Spine迁移与接入.md`「为什么砍掉拟人主宠」）。
- 同体型内的大小/胖瘦/花色差异 = **换 skin + 缩放 + 换部件图**，只有身体结构变
  （四足 → 直立人形）才需要另起骨架，而本项目不做那种角色。
- 换装深度靠**花色数量**，不靠穿搭组合——四足只有 `hat` / `neck` 两个挂点，
  接口的 `Slot` 也只有 `body｜hat｜neck｜bg`。新增外观优先出花色。
- 换装的正解是**把每套外观做成 Spine 命名皮肤**，运行时 `setSkin('002')` 一行搞定。
  反例见下面「别重建 Unity 换装系统」。

## 猫狗怎么区分：狗用小立耳（2026-09-07 定）

判据是「缩到手机上约 250 px 之后还能不能一眼分辨」，对比图见
`docs/狗狗耳型对比-垂耳半垂立耳.png`（三档耳型各带缩小样）。

**结论：狗用小立耳**——短、厚、圆尖、间距开，明显比猫的高窄尖三角小一圈。
猫狗的辨识度改由**深色圆鼻头 + 略长吻部 + 张嘴吐舌**承担。

| 耳型 | 小尺寸辨识度 | Spine 代价 |
| --- | --- | --- |
| **立耳**（选用） | 最弱，靠脸补 | **最低**：耳根立着，单根耳骨，猫的耳朵动画曲线原样能用，网格只需把猫的耳朵调宽调圆 |
| 半垂耳 | 好 | 中：折角画进图里即可，耳根仍立着所以曲线仍可复用 |
| 垂耳 | 最好 | **最高**，见下 |

**垂耳的四笔隐藏成本**（决定不用它的真实原因）：

1. 要动得可信就得把耳骨拆成两段 + 权重网格，单根刚体骨转起来像纸片在扇。
2. 猫那套曲线是照「转一个立着的三角」K 的（如 `idle_timid` 的耳朵后压），
   作用在垂耳上方向根本不对，得单独 K 一套——直接破了「猫狗共用动画」这条省钱的根。
3. 垂耳挂在脸颊**前面**，绘制顺序和 `acc_cap` 的遮挡声明冲突：立耳整只藏掉没问题，
   垂耳藏掉狗就没耳朵了，只能按 `docs/06-UI设计规范.md` §5.5 预留的
   「贴合帽型的耳朵变体」再出一份，而挂头的配饰有四件。
4. 每个花色都要单独出耳朵图（atlas 里是 `01/Ear_L`、`01/Ear_L_B`、`02/Ear_L`…），
   耳朵越复杂，乘上花色数（5~10）之后差得越多。

**如果将来要垂耳，有条便宜路**：把垂的形态画进图里、仍当刚体挂单根骨。
这条成立是因为 §5.6 要求 idle 幅度极小（呼吸起伏 3% 左右），这种量级看不出硬板。
风险只在 `race_run` 和 `trick_jump` 这两个大动作上。

> ⚠️ 两条过期结论，别再照着推理：
>
> - `docs/06-UI设计规范.md` §5.1 那句「正面只有耳朵能分辨猫狗」是**3D 低模时代**得出的
>   （没贴图的灰模，脸上没信息）。2D 手绘的脸能承载多得多，所以立耳狗是可行的，
>   代价只是必须把吻部和鼻子做足。**闭嘴的 idle（`idle_hungry`/`idle_sick`/`idle_lazy`）
>   要专门复查辨识度**，那几个状态下没有吐舌可依靠。
> - §5.4 的「骨骼 ≤ 30 根、只剩 2 根余量」是**3D 限制**，来自 Cocos 实时蒙皮的
>   `JOINT_UNIFORM_CAPACITY = 30`。2D Spine 走自己的 runtime，没有这个硬上限，
>   在新骨架里加耳骨段**不用抠预算**。文档里 §5.4 本身已标注「3D 专属、已作废」。

## 版本必须对齐（否则整只不出画面）

- 引擎 `settings/v2/packages/engine.json` 的 spine 版本要**等于素材导出版本**。
  本项目素材是 **3.8.x** → 用 `spine-3.8`（`spine-4.2` 是 `false`，
  4.2 运行时读不了 3.8 骨骼）。**新素材也按 3.8 导出**，别按 Spine 默认的 4.2。
- 改完 spine 模块**必须在编辑器重新导入引擎**，否则白屏（见 `cocos-toolchain`）。

## 资源落位与加载

- 落到 `assets/resources/spine/<name>/`，`skel|json` + `atlas` + `png` **同名同目录**，
  Cocos 按 basename 配对；运行时 `resources.load('spine/<name>/<Base>', sp.SkeletonData)`
  （**不带扩展名**，所以 `.json` 换 `.skel` 不用改代码）。
- 目录即清单：同类的一批资源用 `resources.loadDir('deco_tiles/wall', SpriteFrame, cb)`
  扫，别手写文件名——以后往目录里丢新图就自动生效。
- `.json` 和 `.skel` **二选一**（同时存在会重复占包）。`.skel` 通常小一半
  （本项目 7.79MB → 4.18MB）。换之前必须校验：两份可能是不同版本导出的，
  **hash 不同但动画名/皮肤名一致才可换**，且 atlas 要配套（比 md5）。

## 挂载后四个必检（错一个就画面异常）

1. **premultipliedAlpha**：直通道(straight) PNG 必须 `skel.premultipliedAlpha = false`。
   设 `true` 会把部件透明边(RGB=白/α=0)算成实心白 → 一堆白矩形叠成「四分五裂」白框。
   只有导出为预乘图集才用 `true`。
2. **空 default 皮肤 → 必须 setSkin**。素材的 `default` skin 可能是空的（0 个槽），
   部件全在命名皮肤里；不选皮肤就**整只不可见**。
   **宠物、顾客、家具道具挂载时都要做这个兜底**：
   取第一个非 `default` 的皮肤挂上，没有命名皮肤时是无操作。
   本项目在 pet_cat、Customer、Box 上各踩过一次。
3. **动画名别写死**：用候选表取第一个存在的（`['idle','Idle','Sit_Idle']`）。
   不同素材命名不一（本包是 `Idle` 不是 `idle`）。
4. **走路朝向**：`Walk` 有固定朝向（本素材**朝左**），换向靠翻 `scale.x` 正负号。
   看到「倒着走」就是符号反了。

## 骨骼坐标是「骨架局部空间」，不是屏幕像素

挂点（帽子/饰品）最容易在这里翻车，本项目连错两次：

- `bone.worldX/worldY` 是**骨架自己的坐标系**。本素材整只猫只有 ~106 单位高，
  `Head` 骨实测在 `(18, 37)`。按屏幕像素给偏移（如 +52）会把饰品甩到画面外。
- **偏移和缩放必须和位置在同一个空间**。位置用骨骼局部坐标时，缩放就别再除以
  `petNode` 的放大倍数——除了等于又缩小一遍。
- **别用 `atan2(bone.c, bone.a)` 求朝向**再沿该方向偏移。这根骨实测 `a=0.00, c=0.52`，
  算出来是 90°，"上"变成了水平方向，帽子直接飞到腰侧。猫头不大幅歪时，
  直接加竖直偏移更稳。
- 想让节点能被点/拖：**必须给 UITransform 设非零 `contentSize`**，
  否则触摸命中不到（骨骼渲染不撑 UITransform）。
  触摸落点转局部用 `parentUITransform.convertToNodeSpaceAR(new Vec3(uiX, uiY, 0))`。

## 动画串播与位置约束

- 前摇/后摇用 `addAnimation` 排队（`_Pre` → 主体 → `_Post` → idle），
  Spine 会按顺序接着播，**不用自己算时长**。
- **位置约束别写死数字**。曾把「拖拽后的落点」直接赋给散步基准且无下限，
  往下一拖就把宠物永久压到状态面板下面、只露头顶且回不来。
  下限要由 UI 层按**真实布局**传进来（如状态面板上沿），面板尺寸变了会自动跟着变。

## 现成素材包处理（Unity 扒的猫咖类素材）

- 家具/站点 Spine 的**贴图常藏在同名子目录**里（`fishbowl/fishbowl/Fishbowl.png`）；
  `.skel.skel` / `.atlas.atlas` 双扩展名要改回单扩展名。
- **Unity 导出物用不了，导入前先查**：`sactx-*` 是 Unity 打的图集
  （2048×2048、无切图坐标，Cocos 取不出单个精灵）；无扩展名的序列化数据表
  （`CustomerPartsData`/`VIPOutfitData` 等）也读不了。
  本项目曾导入 18 张 `sactx-*` 共 19.6MB 才发现全不可用。
- **先确认素材是给谁画的**。`customerparts/` 下的帽子/眼镜是按**人头**画的；
  猫骨架的 atlas 里根本没有 `hat/collar/bow` 类槽位——美术没给猫画配饰。
  戴到猫头上只能算凑合，别当成猫的饰品交付。
- **别重建 Unity 的换装系统**。那套「1731 张散图 + 序列化数据表 + 运行时拼装」
  绑死 Unity：规则数据读不了、散图没有摆放/mesh 信息、mesh 部件的顶点权重
  只存在原始皮肤里。Cocos 不是做不到，是**素材组织方式**决定了要重写一整套。
  能做的替代：随机姿势/朝向/体型 + **按槽位染色**（`slot.color`，Spine 原生）。
- 批量导入骨骼：`node _research/import-spine.mjs <包根> <resources/spine>`。
- 竖屏背景由横版墙+地合成：`_research/compose_bg.ps1 -wall .. -floor .. -out ..`。
- 自造分件图 → 去白底 → 备绑骨素材：用 skill `pet-part-rig-prep`。

## 按槽位染色（换装做不了时的退路）

- `skel.findSlot(name).color` 可写（r/g/b 为 0~1），是 Spine 原生能力，
  **不依赖任何外部换装数据**。用来做肤色/发色/衣服色的粗略区分。
- 染色是**乘法混合**，只能压暗不能提亮 → 预设值取偏浅，纯白 = 保持原色。
- 动画里若有槽位颜色关键帧会**每帧覆盖**掉设定值，
  需要在 `update()` 里持续重涂兜底（几十个槽的开销可忽略）。

## 排查方法：先打印真实值，别靠截图猜

动画/皮肤的差异在小屏上肉眼难辨，反复截图问「是不是生效了」很费轮次。
**加一行日志把运行时真实值打出来**（皮肤名列表、动画名列表、实际选中的那个），
一次就能定位。本项目靠 `skins=["1"] pick="1"` 一行确认了顾客只有一个命名皮肤。

同理，**别用手写的二进制解析结论下判断**——曾自己解 `.skel` 得出「只有 1 个皮肤」，
字节可能错位；以运行时 `getRuntimeData()` 打印的为准。

## 静默失败 = 设计缺陷

「换墙纸/换地板」原本只在装修间场景生效，在其他场景点了**毫无反应也无提示**，
无法判断是坏了还是场景不对。这类功能要么自动切到可用状态，要么明确提示，
不要静默 return。

## 地图里的宠物

家庭地图（`ui/MapView.ts`）在大厅层也挂一只 Spine 猫，复用 `spine/pet_cat/Cat`。
挂载要做的兜底和 `PetStage` 完全一样（空 default 皮肤 → `setSkin`、
`premultipliedAlpha = false`、动画名取候选表、朝向翻 `scale.x`）——
**别因为「只是背景里走两步」就省掉**，省掉的结果是整只不可见或一片白框。

猫现在**满地板 2D 游走、会绕开道具、可被长按拖动**（2026-09-18，栅格 A\* 寻路）——
不再是只沿一条横线走。游走范围（`petMinX/maxX/petMinY/petMaxY`）和寻路障碍的
选型结论与踩坑见规则 `pet-roaming-pathfinding`，加/换道具图后重生成障碍遮罩的
流程见 skill `map-pet-pathfinding`。**别写死坐标** —— 世界宽和墙地比例反复改过，
硬编码旧值不报错，只会让猫走进墙里或飘在半空。

**缩放也不能写死**：单屏下用 0.85（约 90px 高）。曾用 1.6，猫占满整个地板、
比旁边的木马大三倍。改缩放时气泡与状态条的偏移要按同一系数跟着改。

**宠物的附件（气泡/状态条/接地阴影/命中框/飘字）跟猫的实时 x/y**，别用固定 `petY` ——
猫满地板走、y 一直变，用固定值会让附件留在原来那条线上。

**宠物不跟视口**：地图可横向平移，拖到两端时看不到猫是预期行为（见 `map-scroll-view`）。

## 本地模拟数据

后端没有的系统（顾客、装修目录等）先在客户端造假数据（如 `ui/showcaseData.ts`），
界面照真实结构写，接后端时只换数据源、UI 不动。

（工程构建/预览/假后端/出包纪律见 `cocos-toolchain`，项目铁律见 `project-overview`。）
