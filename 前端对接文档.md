# warmpet-api 前端对接文档

> 面向：微信小游戏「宠物养成」客户端开发
> 后端：NestJS 11 + PostgreSQL + Redis，部署于 Sealos DevBox，监听 `8080`
> 文档基准：本仓库当前代码（玩家端 13 个 Controller / 60+ 接口）

---

## 一、开始之前

### 1.1 服务地址

| 环境 | Base URL |
| --- | --- |
| 本地联调 | `http://<DevBox 内网或映射地址>:8080` |
| 公网（小游戏正式请求域名） | DevBox 暴露的 HTTPS 域名，需在微信公众平台配置到 **request 合法域名** |

健康检查 `GET /health`，不需要鉴权，用来确认联通性。

> 微信小游戏正式版只允许请求已备案且配置在白名单的 HTTPS 域名。开发阶段可在微信开发者工具勾选「不校验合法域名」。

### 1.2 全局约定

- **请求体一律 JSON**，`Content-Type: application/json`。
- **所有业务接口都要带 JWT**（`/auth/login` 和 `/health` 除外）。
- **服务端权威**：时间、收益、衰减、名次全部由服务端计算。客户端不要上报时间戳、不要上报"我获得了多少币"，这类字段会被 `ValidationPipe` 直接拒绝（见 1.5）。
- **bigint 主键在 JSON 里是字符串**。`petId`、`raceId`、`layoutId`、`addressId`、订单 `id` 都是 string，前端不要用 `Number()` 转换，会在超过 2^53 后丢精度。
- **时间字段是 ISO 8601 字符串**（UTC），如 `"2026-08-26T12:00:00.000Z"`。业务日切按东八区自然日。
- **金额、计数是整数**；`speed`、`endurance`、`finishTime`、`coinPerHour`、`comfortFactor` 是小数。

### 1.3 鉴权

登录拿到 token 后，之后每个请求带：

```
Authorization: Bearer <token>
```

Token 默认有效期 **7 天**（`JWT_EXPIRES_IN`）。收到 `401` 就重新走一次登录流程。

**封禁账号的行为**：被封禁的玩家 **读接口（GET）仍可访问**，写接口（POST/PUT/DELETE）返回 `403`。这是有意设计——让被封玩家能看到自己的数据以便申诉。前端遇到 403 应弹出封禁提示而不是静默失败。

### 1.4 统一错误格式

任何异常都是这个形状（`AllExceptionsFilter`）：

```json
{ "code": 400, "message": "余额不足" }
```

参数校验失败时 `message` 是**字符串数组**：

```json
{ "code": 400, "message": ["bizId must be a string", "itemKey should not be empty"] }
```

`code` 与 HTTP 状态码一致。前端封装时建议：`message` 若为数组取第一条展示，否则直接展示（后端的 message 都是可直接展示给玩家的中文）。

### 1.5 参数校验很严格（重要）

全局 `ValidationPipe` 开了 `whitelist: true, forbidNonWhitelisted: true`。意思是：

- **多传任何未定义的字段会直接 400**，不是忽略。比如给 `POST /pet/feed` 多传一个 `timestamp` 会报错。
- 这条对 **query 参数同样生效**。`GET /pet/state?petId=1&foo=2` 会 400。

所以请求体请严格按本文档列的字段传，不要顺手带上客户端的调试字段。

### 1.6 幂等与 `bizId`

所有涉及**发放/扣减资源**的写接口都要求传 `bizId`——由客户端生成的全局唯一操作 ID。

工作机制：

1. 服务端用 `idem:{userId}:{bizId}` 抢 Redis 占位。
2. 抢到 → 执行业务，成功后把响应体缓存 **24 小时**。
3. 没抢到且上次还在处理中 → 返回 **409 `请求处理中，请勿重复提交`**。
4. 没抢到但上次已完成 → **回放上次的响应体**（不会重复扣费/发奖）。

经济类操作还在数据库层叠加了唯一索引做持久去重，所以就算超过 24 小时重放也不会重复发钱。

**前端该怎么做**：

- 每次**用户主动发起**的操作生成一个新 `bizId`（推荐 `${action}:${Date.now()}:${随机串}`，或用 uuid）。
- **网络超时重试时复用同一个 `bizId`**——这正是幂等的意义所在。换新 ID 重试等于发起一笔新交易。
- 遇到 `409` 说明上一次还在处理，等 1~2 秒后**带同一个 bizId** 重试即可拿到结果。
- 部分接口的响应里有 `duplicated: true`，表示这次是回放而非真实执行，前端不要重复播放"获得奖励"动画。

`bizId` 最长 128 字符（部分接口 64，见各接口说明）。

### 1.7 分页

统一 query 参数与响应形状：

- 入参：`page`（1 起，默认 1）、`pageSize`（默认 20，**最大 100**）
- 出参：`{ "list": [...], "total": 123 }`

---

## 二、登录接入

### 2.1 流程

```
wx.login() → code → POST /auth/login → { token, userId } → 本地缓存 token
```

### 2.2 `POST /auth/login`

无需鉴权。

**请求**

```json
{ "code": "微信 wx.login 返回的 code" }
```

**响应**

```json
{
  "token": "eyJhbGciOi...",
  "userId": "1"
}
```

**异常**

| HTTP | message | 说明 |
| --- | --- | --- |
| 401 | `微信登录失败：{微信错误信息}` | code 无效/已使用/过期 |
| 403 | `账号已被封禁` 或 `账号已被封禁：{原因}` | 封禁账号不签发新令牌 |

首次登录会自动建号。身份认定优先用 `unionid`，没有则用 `openid`。

### 2.3 客户端参考实现

```js
// game/net/auth.js
const BASE_URL = 'https://your-api-domain.example.com';

let token = wx.getStorageSync('warmpet_token') || '';

export function getToken() {
  return token;
}

export function login() {
  return new Promise((resolve, reject) => {
    wx.login({
      success: ({ code }) => {
        wx.request({
          url: `${BASE_URL}/auth/login`,
          method: 'POST',
          data: { code },
          success: (res) => {
            if (res.statusCode !== 200) return reject(res.data);
            token = res.data.token;
            wx.setStorageSync('warmpet_token', token);
            resolve(res.data);
          },
          fail: reject,
        });
      },
      fail: reject,
    });
  });
}
```

### 2.4 假登录（联调用）

后端支持免真机登录，方便在没有微信客户端的情况下调通接口。

后端 `.env` 设 `WECHAT_MOCK_LOGIN=true` 后，`POST /auth/login` 传 `{"code": "mock:alice"}` 即可跳过微信校验直接签发 JWT。同一个 tag 永远映射同一个玩家。

标识只允许 `[A-Za-z0-9_-]{1,32}`。生产环境（`NODE_ENV=production`）这个开关会被强制关闭，应用甚至会拒绝启动，所以不用担心误开。

后端提供了三个预置测试账号（`npm run seed:dev`）：

| code | 进度 | 适合调试 |
| --- | --- | --- |
| `mock:newbie` | Lv1，空钱包 | 新手引导 / 首次互动 |
| `mock:mid` | Lv8，5000 币 | 换装 / 家园 / 赛跑 / 签到 |
| `mock:veteran` | Lv30，5 万币 | 兑换实物 / 履约 / 图鉴 |

用 curl 快速验证：

```bash
curl -s -X POST http://localhost:8080/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"code":"mock:mid"}'
```

---

## 三、接口速查表

| 模块 | 方法 | 路径 | 幂等 | 说明 |
| --- | --- | --- | --- | --- |
| 系统 | GET | `/health` | — | 健康检查（免鉴权） |
| 鉴权 | POST | `/auth/login` | — | 微信登录（免鉴权） |
| 宠物 | GET | `/pet/state` | — | 出战宠结算后状态 |
| 宠物 | GET | `/pet/list` | — | 我的宠物列表 |
| 宠物 | GET | `/pet/offline` | — | 离线收益预览 |
| 宠物 | POST | `/pet/offline/claim` | ✓ | 领取离线收益 |
| 宠物 | POST | `/pet/create` | ✓ | 新建宠物 |
| 宠物 | PUT | `/pet/active` | — | 切换出战宠 |
| 宠物 | POST | `/pet/feed` | ✓ | 喂食 |
| 宠物 | POST | `/pet/bath` | ✓ | 洗澡 |
| 宠物 | POST | `/pet/pet` | ✓ | 抚摸 |
| 宠物 | POST | `/pet/play` | ✓ | 陪玩（消耗体力） |
| 钱包 | GET | `/wallet` | — | 双币余额 |
| 钱包 | GET | `/wallet/ledger` | — | 流水（分页） |
| 每日 | GET | `/daily` | — | 签到状态 + 任务列表 |
| 每日 | POST | `/daily/checkin` | ✓ | 签到 |
| 每日 | POST | `/daily/task/claim` | ✓ | 领取任务奖励 |
| 赛跑 | GET | `/race/tracks` | — | 赛道 + 战力预览 |
| 赛跑 | POST | `/race/start` | ✓ | 报名参赛 |
| 赛跑 | POST | `/race/settle` | ✓ | 结算领奖 |
| 赛跑 | POST | `/race/reward/double` | ✓ | 看广告奖励翻倍 |
| 赛跑 | POST | `/race/revive` | ✓ | 看广告复活重跑 |
| 广告 | POST | `/ad/token` | — | 领取一次性广告凭证 |
| 广告 | POST | `/ad/verify` | ✓ | 核销广告奖励 |
| 增值 | POST | `/boost/speedup` | ✓ | 花币清冷却 |
| 增值 | POST | `/stamina/recover` | ✓ | 花币回体力 |
| 换装 | GET | `/wardrobe` | — | 商店 + 拥有/穿戴态 |
| 换装 | POST | `/wardrobe/buy` | ✓ | 购买 |
| 换装 | POST | `/wardrobe/equip` | — | 穿戴 |
| 换装 | POST | `/wardrobe/unequip` | — | 卸下 |
| 消耗品 | GET | `/items/consumables` | — | 消耗品商店 |
| 消耗品 | POST | `/items/consumables/buy` | ✓ | 购买 |
| 消耗品 | POST | `/items/consumables/use` | ✓ | 使用一份 |
| 家园 | GET | `/home` | — | 家具 + 布局 + 舒适度 |
| 家园 | POST | `/home/buy` | ✓ | 购买家具 |
| 家园 | POST | `/home/place` | — | 摆放 |
| 家园 | POST | `/home/remove` | — | 收纳 |
| 图鉴 | GET | `/dex` | — | 图鉴进度 |
| 图鉴 | POST | `/dex/claim` | ✓ | 领取图鉴奖励 |
| 扭蛋 | GET | `/gacha` | — | 奖池 + **概率公示** + 保底进度 |
| 扭蛋 | GET | `/gacha/history` | — | 抽奖记录（分页） |
| 扭蛋 | POST | `/gacha/draw` | ✓ | 抽奖（1 或 10 连） |
| 兑换 | GET | `/exchange` | — | 兑换目录 |
| 兑换 | GET | `/exchange/orders` | — | 我的订单（分页） |
| 兑换 | POST | `/exchange/redeem` | ✓ | 下单兑换 |
| 地址 | GET | `/address` | — | 地址列表 |
| 地址 | POST | `/address` | — | 新增地址 |
| 地址 | PUT | `/address/:id` | — | 修改地址 |
| 地址 | DELETE | `/address/:id` | — | 删除地址 |
| 兑换码 | POST | `/promo/redeem` | ✓ | 核销兑换码 |
| 兑换码 | GET | `/promo/redemptions` | — | 核销记录（分页） |

---

## 四、核心数据结构

### 4.1 `PetStateView` — 宠物状态

出现在 `/pet/*`、`/stamina/recover`、`/items/consumables/use` 的响应里。**所有属性都是服务端惰性结算后的当前值**，前端直接渲染即可。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 宠物 ID |
| `nickname` | string \| null | 昵称 |
| `species` | string | 物种，默认 `"default"` |
| `isActive` | boolean | 是否出战宠 |
| `hunger` | number | 饱食度 **0–100** |
| `cleanliness` | number | 清洁度 **0–100** |
| `mood` | number | 心情 **0–100** |
| `stamina` | number | 当前体力 **0–staminaMax** |
| `staminaMax` | number | 体力上限（随等级增长） |
| `intimacy` | number | 亲密度，无上限 |
| `level` | number | 等级 1–100 |
| `exp` | number | 累计经验 |
| `expIntoLevel` | number | 当前等级内已累积经验 |
| `expToNext` | number | 升级还需经验，**满级为 0** |
| `stage` | string | 成长阶段：`baby`(≤5) / `teen`(≤15) / `adult`(≤100) |
| `speed` | number | 速度（仅由等级派生，1 位小数） |
| `endurance` | number | 耐力（仅由等级派生，1 位小数） |
| `lastSeenAt` | string | ISO8601，上次结算锚点 |

进度条渲染：等级经验条用 `expIntoLevel / (expIntoLevel + expToNext)`，满级时特殊处理（`expToNext === 0`）。

### 4.2 `WalletView` — 钱包

```json
{ "gameCoin": 1200, "marketingPoint": 50 }
```

**两个积分池物理隔离，接口层禁止互转**：

| 池 | 字段 | 来源 | 用途 |
| --- | --- | --- | --- |
| `game` 游戏币 | `gameCoin` | 玩法产出（互动/离线/签到/赛跑/广告/图鉴） | 商店购买、扭蛋、部分兑换 |
| `marketing` 营销积分 | `marketingPoint` | 兑换码核销、后台发放 | 实物兑换、代金券 |

很多写接口的响应会顺带返回 `gameCoin` 或完整 `wallet`，前端可以直接更新本地余额，不必额外请求 `/wallet`。

### 4.3 `BuyResult` — 购买通用返回

换装 / 家园 / 消耗品的 `buy` 共用：

```json
{
  "itemKey": "acc_cap",
  "qty": 1,
  "wallet": { "gameCoin": 900, "marketingPoint": 0 },
  "duplicated": false
}
```

`qty` 是**购买后的持有量**（不是本次买了几件）。`duplicated: true` 表示幂等回放。

---

## 五、宠物模块 `/pet`

### 5.1 `GET /pet/state`

Query：`petId`（可选，string；省略则取出战宠）

响应 `{ "pet": PetStateView }`

> 玩家还没有宠物时，这个接口会**自动创建一只**并返回，前端不需要处理"无宠"分支。

### 5.2 `GET /pet/list`

响应 `{ "pets": PetStateView[] }`

与 `/pet/state` 不同，这个接口**不会自动建宠**。全新玩家如果先调 `/pet/list` 会拿到空数组，所以冷启动时应该先请求 `/pet/state`。

### 5.3 `PUT /pet/active`

请求 `{ "petId": "3" }` → 响应 `{ "pet": PetStateView }`。无 `bizId`（切换出战宠不涉及资源变动）。

### 5.4 `POST /pet/create`

请求：

```json
{ "bizId": "create:1756200000000:a1b2", "nickname": "小白", "species": "default" }
```

`nickname` / `species` 可选，各限 32 字符。响应 `{ "pet": PetStateView }`。

最多养 **6 只**，超出返回 400 `最多只能养 6 只宠物`。

### 5.5 四种互动 `POST /pet/{feed|bath|pet|play}`

请求：

```json
{ "bizId": "feed:1756200000000:a1b2", "petId": "3" }
```

`petId` 可选，省略作用于出战宠。

响应：

```json
{
  "pet": { "...": "PetStateView" },
  "gained": { "intimacy": 2, "exp": 5, "coin": 3 },
  "capped": false,
  "levelUp": false,
  "cooldownRemainMs": 30000,
  "gameCoin": 1203
}
```

| 字段 | 说明 |
| --- | --- |
| `gained` | 本次**实发**数值（已扣掉每日上限截断的部分） |
| `capped` | true 表示撞到每日上限，实发少于标称值。前端应提示"今日收益已达上限" |
| `levelUp` | 是否升级，用于触发升级动画 |
| `cooldownRemainMs` | 该动作的完整冷却时长（毫秒），前端据此起本地倒计时 |
| `gameCoin` | 操作后的游戏币余额 |

**四个动作的默认参数**：

| 动作 | 路径 | 属性变化 | 产出 | 冷却 |
| --- | --- | --- | --- | --- |
| 喂食 | `/pet/feed` | 饱食 +30 | 亲密 2 / 经验 5 / 币 3 | 30 秒 |
| 洗澡 | `/pet/bath` | 清洁 +40 | 亲密 2 / 经验 5 / 币 3 | 60 秒 |
| 抚摸 | `/pet/pet` | 心情 +15 | 亲密 3 / 经验 3 / 币 2 | 20 秒 |
| 陪玩 | `/pet/play` | 心情 +20，**体力 −10** | 亲密 4 / 经验 8 / 币 5 | 60 秒 |

**每日产出上限（账号级，多宠共享）**：亲密度 100 / 经验 200 / 游戏币 500。

**异常**：

| HTTP | message | 前端处理 |
| --- | --- | --- |
| 429 | `冷却中，还需 N 秒` | 按钮置灰并倒计时。理想情况前端本地倒计时就能避免这个错误 |
| 400 | `体力不足` | 引导去 `/stamina/recover` 或等自然恢复 |
| 404 | `宠物不存在` | petId 失效，刷新列表 |
| 400 | `最多只能养 6 只宠物` | create 超限 |

### 5.6 衰减规则（前端做进度条预测用）

服务端是**惰性结算**：不跑定时任务，每次读写时按 `lastSeenAt` 到现在的时长算。前端如果想在本地平滑推进进度条，用下面的公式（`elapsedH` = 距 `lastSeenAt` 的小时数）：

```
hunger      = clamp(hunger      - 5 * elapsedH,  0, 100)   // 5/小时
cleanliness = clamp(cleanliness - 3 * elapsedH,  0, 100)   // 3/小时
stamina     = clamp(stamina     + 10 * elapsedH, 0, staminaMax)  // 恢复 10/小时
```

心情复杂一些——饿肚子或脏了之后会**加速掉心情**，家园舒适度可以减缓：

```
hungerZeroH = hunger / 5
cleanZeroH  = cleanliness / 3
starvingH   = max(0, elapsedH - min(hungerZeroH, cleanZeroH))
moodDecay   = (2 * elapsedH + 3 * starvingH) * (1 - comfortFactor)
mood        = clamp(mood - moodDecay, 0, 100)
```

`comfortFactor` 来自 `GET /home`，范围 0–0.3。

本地预测只用于视觉平滑，**任何数值判定都以服务端返回为准**。

### 5.7 离线收益

**`GET /pet/offline`** 预览：

```json
{
  "elapsedSec": 36000,
  "cappedSec": 28800,
  "maxHours": 8,
  "coinPerHour": 14.3,
  "comfortFactor": 0.3,
  "claimableCoin": 114
}
```

`elapsedSec` 是实际离线秒数，`cappedSec` 是封顶后的（最多 **8 小时**）。前端应展示"离线收益已满，快来领取"的提示当 `elapsedSec > maxHours * 3600` 时。

**`POST /pet/offline/claim`** 领取，请求 `{ "bizId": "..." }`，响应：

```json
{ "gained": 114, "gameCoin": 1317 }
```

`claimableCoin < 1` 时领取返回 400 `暂无可领取的离线收益`，前端应在预览值为 0 时禁用领取按钮。

时薪计算：`10 * (1 + 0.05 * (level - 1)) * (1 + comfortFactor)`。等级和家园舒适度都能提升。

---

## 六、钱包模块 `/wallet`

### 6.1 `GET /wallet`

```json
{ "wallet": { "gameCoin": 1200, "marketingPoint": 50 } }
```

### 6.2 `GET /wallet/ledger`

Query：`page`、`pageSize`、`pool`（可选，`game` | `marketing`）

```json
{
  "list": [
    {
      "id": "128",
      "pool": "game",
      "delta": 30,
      "balanceAfter": 1230,
      "bizId": "ad:uuid",
      "reason": "ad",
      "refId": "20260826",
      "createdAt": "2026-08-26T12:00:00.000Z"
    }
  ],
  "total": 57
}
```

`delta` 正为收入、负为支出；`balanceAfter` 是该笔之后的余额。

**`reason` 枚举**（用于流水页的图标/文案映射）：

| reason | 含义 | reason | 含义 |
| --- | --- | --- | --- |
| `interact` | 互动照顾 | `purchase` | 购买消费 |
| `offline` | 离线收益 | `exchange` | 兑换消费 |
| `race` | 赛跑 | `promo` | 兑换码 |
| `daily` | 签到/任务 | `gacha` | 扭蛋 |
| `ad` | 广告奖励 | `admin_grant` | 后台发放 |
| `boost` | 加速/回体力 | `admin_deduct` | 后台扣减 |
| `dex` | 图鉴奖励 | `compensation` | 补偿 |

> 没有通用的"加币"接口。所有发放都由各玩法在自己的业务语义里完成，这是为了杜绝"凭空加币"的攻击面。

---

## 七、每日模块 `/daily`

### 7.1 `GET /daily`

```json
{
  "checkin": {
    "done": false,
    "streak": 3,
    "totalCheckins": 12,
    "todayReward": 40,
    "nextReward": 50
  },
  "tasks": [
    { "key": "checkin",  "name": "完成每日签到", "target": 1, "progress": 0, "coin": 10, "done": false, "claimed": false },
    { "key": "interact", "name": "照顾宠物 5 次", "target": 5, "progress": 2, "coin": 30, "done": false, "claimed": false },
    { "key": "play",     "name": "陪玩 3 次",     "target": 3, "progress": 0, "coin": 20, "done": false, "claimed": false },
    { "key": "race",     "name": "完成 1 场赛跑", "target": 1, "progress": 0, "coin": 25, "done": false, "claimed": false }
  ]
}
```

`progress` 已经被 clamp 到不超过 `target`，进度条直接用 `progress / target`。

**签到奖励**：`min(100, 20 + (streak - 1) * 10)`，连签越久越多，封顶 100。

**任务进度来源**：`interact` 累计任意互动次数，`play` 只算陪玩，`race` 在**首次结算**赛跑时 +1，`checkin` 跟随签到状态。

> 任务列表是运营可配置的，后台可以随时增删。前端**不要硬编码 taskKey 列表**，按接口返回的数组渲染。

### 7.2 `POST /daily/checkin`

请求 `{ "bizId": "..." }`（bizId 只用于请求级去重，真正的幂等键由服务端按业务日派生，所以就算前端 bizId 乱传也不会重复发奖）。

响应 `{ "daily": DailyView, "gained": 40, "gameCoin": 1240 }`

`daily` 是刷新后的完整 `GET /daily` 结构，前端可以直接替换本地状态。

异常：400 `今日已签到`。

### 7.3 `POST /daily/task/claim`

请求 `{ "bizId": "...", "taskKey": "interact" }`，响应同上。

异常：400 `未知任务` / `该任务今日已领取` / `任务尚未完成`。

---

## 八、赛跑模块 `/race`

### 8.1 状态机

```
POST /race/start  ──→  pending  ──POST /race/settle──→  settled
                          │                                 │
                          │ POST /race/revive               │ POST /race/reward/double
                          │ （看广告，重算成绩，仍 pending）  │ （看广告，再发等额奖励）
                          └──────────┘                      └──────────┘
```

关键约束：

- **`revive` 只能在 `pending` 时调用**，每场最多 1 次，不再扣体力和门票。
- **`reward/double` 只能在 `settled` 之后调用**，且 `rewardCoin > 0`。
- 名次和奖励在 `start` 时就已算定，`settle` 只是发奖。前端可以在 `start` 返回后先播赛跑动画，播完再调 `settle`。

### 8.2 `GET /race/tracks`

```json
{
  "tracks": [
    {
      "key": "meadow", "name": "新手草地",
      "distance": 100, "difficulty": 1.0,
      "staminaCost": 20, "entryCoin": 0,
      "baseReward": 50, "recommendLevel": 1, "targetTime": 24
    }
  ],
  "battle": {
    "petId": "3", "nickname": "小白", "level": 8,
    "power": 42, "stamina": 80, "staminaMax": 114
  }
}
```

`battle` 在无宠时为 `null`（不报错）。`power = speed * 2 + endurance`，**仅供展示**，不参与名次计算。

**默认赛道**：

| key | 名称 | 距离 | 体力 | 门票 | 基准奖励 | 建议等级 | 基准时间 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `meadow` | 新手草地 | 100 | 20 | 0 | 50 | 1 | 24s |
| `forest` | 密林赛道 | 200 | 35 | 20 | 120 | 5 | 36s |
| `mountain` | 峭壁险道 | 300 | 50 | 50 | 250 | 12 | 37s |

赛道也是运营可配置的，前端按返回值渲染，不要硬编码。

### 8.3 `POST /race/start`

请求 `{ "bizId": "...", "trackKey": "meadow", "petId": "3" }`（`petId` 可选）

```json
{
  "raceId": "77",
  "trackKey": "meadow",
  "rank": 2,
  "totalRacers": 4,
  "finishTime": 23.456,
  "grade": "A",
  "opponentFinishTimes": [22.1, 25.3, 26.0],
  "ghostSource": "player",
  "playerScore": 42,
  "staminaLeft": 60,
  "status": "pending"
}
```

**播放赛跑动画就用 `finishTime` 和 `opponentFinishTimes`**（升序数组，秒，3 位小数）。把玩家和 3 个影子的完赛时间映射成动画时长即可，名次自然对得上。

`grade` 是 `S`/`A`/`B`/`C`，按 `finishTime / targetTime` 判定（≤0.9 为 S，≤1.0 为 A，≤1.15 为 B，更慢 C）。

`ghostSource` 是影子对手的来源（`player` / `mixed` / `npc`），可用于展示"对手来自真实玩家"之类的文案。

**注意**：`start` 用新 `bizId` 调用就是开新的一局，会**再次扣体力和门票**。不要在重试时换 `bizId`。

**异常**：400 `未知赛道` / `游戏币不足以支付报名门票` / `体力不足，无法参赛`，404 `没有可参赛的宠物`。

### 8.4 `POST /race/settle`

请求 `{ "bizId": "...", "raceId": "77" }`

```json
{
  "raceId": "77", "rank": 2, "totalRacers": 4,
  "finishTime": 23.456, "grade": "A",
  "rewardCoin": 30, "gameCoin": 1270,
  "duplicated": false
}
```

`duplicated: true` 表示已结算过，本次是回放，**不要重复播放领奖动画**。

名次奖励系数 `[1, 0.6, 0.35, 0.15]`，`rewardCoin = round(baseReward * 系数)`。

### 8.5 `POST /race/reward/double`（看广告翻倍）

前置：先 `POST /ad/token` 拿 `scene=race_double` 的凭证，播完激励视频再调。

请求 `{ "bizId": "...", "raceId": "77", "adToken": "<nonce>" }`

```json
{ "raceId": "77", "bonusCoin": 30, "totalRewardCoin": 60, "gameCoin": 1300, "duplicated": false }
```

异常：400 `请先结算本场比赛再翻倍` / `本场无奖励可翻倍` / `广告凭证无效或已过期`。

### 8.6 `POST /race/revive`（看广告复活重跑）

前置：`scene=race_revive` 的凭证，且该场仍为 `pending`。

请求 `{ "bizId": "...", "raceId": "77", "adToken": "<nonce>" }`

```json
{
  "raceId": "77", "rank": 1, "totalRacers": 4,
  "finishTime": 22.1, "grade": "S",
  "opponentFinishTimes": [23.5, 24.0, 25.1],
  "ghostSource": "npc", "playerScore": 42,
  "rewardCoin": 50,
  "previousRank": 4, "previousFinishTime": 28.5,
  "reviveCount": 1,
  "status": "pending"
}
```

重跑会用**当前**宠物属性重算，完赛时间有 5% 的加成。重跑后仍是 `pending`，需要再调一次 `settle` 才拿奖。

异常：400 `已结算的比赛不能复活重跑` / `本场复活机会已用完`。

---

## 九、广告与增值 `/ad` `/boost` `/stamina`

### 9.1 广告凭证机制

**为什么需要凭证**：如果客户端说"我看完广告了"服务端就发币，任何人抓包就能无限刷。所以采用"先领一次性 nonce，播完广告拿 nonce 核销"的两步流程。

```
POST /ad/token { scene }  →  { nonce, expiresInSec, remaining }
        ↓ 客户端播放激励视频（必须在 TTL 内完成，默认 300 秒）
POST /ad/verify | /race/reward/double | /race/revive  { adToken: nonce, ... }
        ↓ nonce 立即失效，scene 必须与领取时一致
```

**`POST /ad/token`**

请求 `{ "scene": "ad_reward" }`，响应：

```json
{ "nonce": "3f2a...", "scene": "ad_reward", "expiresInSec": 300, "remaining": 4 }
```

**这个接口不幂等**——每次调用签发一枚新凭证，受该 scene 的每日上限（默认 10 次/天）约束。不要在页面加载时预先批量领取。

| scene | 用途 | 核销接口 |
| --- | --- | --- |
| `ad_reward` | 激励视频直接发币 | `POST /ad/verify` |
| `race_double` | 赛跑奖励翻倍 | `POST /race/reward/double` |
| `race_revive` | 赛跑复活重跑 | `POST /race/revive` |

**`POST /ad/verify`**

请求 `{ "bizId": "...", "adToken": "<nonce>" }`，响应：

```json
{ "gained": 30, "gameCoin": 1330, "remaining": 4 }
```

`remaining` 是今日还能领几次广告奖励（默认上限 5 次/天）。

> **一个需要注意的坑**：`verify` 是**先核销 token 再发币**。如果发币成功但客户端没收到响应，带同一个 `bizId` 重试会被幂等层回放（正常）；但如果重新领了新 token 再试，会因为原 token 已用而失败。所以网络异常时的正确做法是**用同一 bizId 重试**，或者干脆去拉一次 `GET /wallet` 确认到账。

### 9.2 `POST /boost/speedup` — 花币清冷却

请求 `{ "bizId": "...", "petId": "3" }`（`petId` 可选）

```json
{ "cleared": 4, "gameCoin": 1180 }
```

一次清除该宠物**全部 4 个动作**的冷却，默认花费 20 游戏币。`cleared` 是实际清掉的冷却数量。

### 9.3 `POST /stamina/recover` — 花币回体力

请求 `{ "bizId": "...", "petId": "3" }`，响应 `{ "pet": PetStateView, "gameCoin": 1130 }`

默认花费 50 游戏币，体力回满。

两个接口余额不足时都返回 400 `余额不足`。

---

## 十、换装模块 `/wardrobe`

### 10.1 `GET /wardrobe`

Query：`petId`（可选）

```json
{
  "petId": "3",
  "items": [
    { "key": "skin_default", "type": "skin", "name": "原色", "slot": "body", "price": 0, "pool": "game", "owned": true, "equipped": true },
    { "key": "acc_cap", "type": "accessory", "name": "棒球帽", "slot": "hat", "price": 300, "pool": "game", "owned": false, "equipped": false }
  ],
  "equipped": { "body": "skin_default", "hat": "acc_cap" }
}
```

`equipped` 是 **槽位 → itemKey** 的映射，渲染角色形象时按槽位取图即可。`price === 0` 的物品视为默认拥有。

**槽位（slot）**：`body`（皮肤）、`hat`（头部配饰）、`neck`（颈部配饰）、`bg`（背景）。每个槽位同时只能穿一件。

**默认商品**：

| 类型 | itemKey | 名称 | slot | 价格 |
| --- | --- | --- | --- | --- |
| 皮肤 | `skin_default` | 原色 | body | 0 |
| 皮肤 | `skin_snow` | 雪白 | body | 400 |
| 皮肤 | `skin_tiger` | 虎纹 | body | 900 |
| 皮肤 | `skin_calico` | 三花 | body | 1400 |
| 皮肤 | `skin_shadow` | 玄影 | body | 2200 |
| 配饰 | `acc_cap` | 棒球帽 | hat | 300 |
| 配饰 | `acc_bow` | 蝴蝶结 | hat | 450 |
| 配饰 | `acc_glasses` | 墨镜 | hat | 700 |
| 配饰 | `acc_crown` | 小皇冠 | hat | 1600 |
| 配饰 | `acc_bell` | 铃铛 | neck | 350 |
| 配饰 | `acc_bandana` | 领巾 | neck | 500 |
| 配饰 | `acc_scarf` | 围巾 | neck | 600 |
| 背景 | `bg_room` | 暖阳小屋 | bg | 0 |
| 背景 | `bg_garden` | 午后花园 | bg | 800 |
| 背景 | `bg_beach` | 海边夕照 | bg | 1200 |
| 背景 | `bg_starry` | 星夜露台 | bg | 1800 |

美术资源命名建议直接对齐 `itemKey`，这样新增商品时前端只要补图不用改代码。

> **换装只影响外观，不影响任何属性**。速度和耐力只由等级决定。

### 10.2 `POST /wardrobe/buy`

请求 `{ "bizId": "...", "itemKey": "acc_cap" }`，响应 `BuyResult`。

异常：400 `该物品无需购买`（免费物品）/ `余额不足`，404 `物品不存在或已下架`。

### 10.3 `POST /wardrobe/equip` / `POST /wardrobe/unequip`

- equip 请求 `{ "itemKey": "acc_cap", "petId": "3" }`
- unequip 请求 `{ "slot": "hat", "petId": "3" }`

两者都返回完整的 `WardrobeView`，前端直接替换本地状态即可。不需要 `bizId`（不涉及资源变动）。

异常：400 `尚未拥有该物品` / `该物品不可穿戴`，404 `没有可穿戴的宠物`。

---

## 十一、消耗品 `/items/consumables`

### 11.1 `GET /items/consumables`

```json
{
  "items": [
    { "key": "cons_snack", "name": "宠物零食", "price": 60, "pool": "game",
      "effect": { "hunger": 25 }, "owned": 3, "sortOrder": 1 }
  ],
  "wallet": { "gameCoin": 1200, "marketingPoint": 0 }
}
```

**默认消耗品**：

| itemKey | 名称 | 价格 | 效果 |
| --- | --- | --- | --- |
| `cons_toy` | 玩具球 | 50 | 心情 +20 |
| `cons_snack` | 宠物零食 | 60 | 饱食 +25 |
| `cons_bubble` | 清洁泡泡 | 60 | 清洁 +30 |
| `cons_energy` | 能量饮 | 120 | 体力 +40 |
| `cons_cake` | 生日蛋糕 | 300 | 饱食 +20，心情 +25，经验 +30 |

### 11.2 `POST /items/consumables/buy`

请求 `{ "bizId": "...", "itemKey": "cons_snack", "qty": 5 }`（`qty` 默认 1，上限 99），响应 `BuyResult`。

### 11.3 `POST /items/consumables/use`

请求 `{ "bizId": "...", "itemKey": "cons_snack", "petId": "3" }`

```json
{
  "itemKey": "cons_snack",
  "left": 4,
  "effect": { "hunger": 25 },
  "pet": { "...": "PetStateView" },
  "levelUp": false
}
```

**一次只能用一份**，接口不支持批量。这是有意的——批量使用时"喂到饱食度上限就该停"会变成服务端猜测，多喂的部分被 clamp 掉等于玩家的道具凭空消失。要连喂就让玩家连点。

消耗品使用**没有冷却**，和四种互动动作不同。

异常：400 `该消耗品数量不足`，404 `消耗品不存在`。

---

## 十二、家园模块 `/home`

### 12.1 `GET /home`

```json
{
  "comfort": 45,
  "comfortFactor": 0.3,
  "grid": { "width": 6, "height": 6 },
  "items": [
    { "key": "furn_sofa", "name": "沙发", "price": 900, "pool": "game",
      "comfort": 15, "owned": 2, "placed": 1, "gridW": 2, "gridH": 1 }
  ],
  "placed": [
    { "layoutId": "12", "itemKey": "furn_sofa", "name": "沙发",
      "comfort": 15, "posX": 1, "posY": 2, "gridW": 2, "gridH": 1 }
  ]
}
```

- `grid` 是房间网格尺寸（默认 6×6），坐标从 **0 起，左上角为原点**。
- `items[].owned` 是背包持有数，`placed` 是已摆放数。可摆放数 = `owned - placed`。
- `placed[].layoutId` 是摆放记录 ID，收纳时要传它。
- 同一件家具可以买多份、摆多份。

**默认家具**：

| itemKey | 名称 | 价格 | 舒适度 | 占格 |
| --- | --- | --- | --- | --- |
| `furn_bowl` | 食盆 | 200 | 5 | 1×1 |
| `furn_mat` | 暖垫 | 250 | 8 | 2×2 |
| `furn_lamp` | 落地灯 | 550 | 10 | 1×1 |
| `furn_rug` | 圆形地毯 | 700 | 12 | 2×2 |
| `furn_sofa` | 沙发 | 900 | 15 | 2×1 |
| `furn_tree` | 猫爬架 | 1300 | 20 | 1×2 |
| `furn_window` | 落地窗 | 1600 | 18 | 1×2 |

### 12.2 舒适度的作用

`comfortFactor = min(comfort / 100, 0.3)`，即舒适度堆到 100 就到上限 0.3，再堆没有额外收益（前端可以在 UI 上标出这个上限，避免玩家白花钱）。

它影响两件事：

1. **心情衰减减缓**：`comfortFactor = 0.3` 时心情掉得只有原来的 70%。
2. **离线时薪加成**：时薪 ×`(1 + comfortFactor)`，最多 +30%。

不影响饱食/清洁/体力的变化，也不影响赛跑属性。

### 12.3 `POST /home/buy` / `place` / `remove`

- **buy** 请求 `{ "bizId": "...", "itemKey": "furn_sofa" }` → `BuyResult`
- **place** 请求 `{ "itemKey": "furn_sofa", "posX": 1, "posY": 2 }` → `HomeView`
  坐标可以省略，服务端会自动找空位。
- **remove** 请求 `{ "layoutId": "12" }` → `HomeView`

place / remove 都返回完整 `HomeView`，前端直接替换本地状态。

异常：400 `可摆放数量不足，请先购买` / `该位置已有家具，请换个位置` / `摆放位置超出房间范围（房间 6×6，该家具占 2×1）` / `房间已摆满，请先收纳部分家具`。

拖拽摆放时建议前端先做本地碰撞检测（用 `placed` 数组 + `gridW/gridH`），把服务端的 400 当兜底。

---

## 十三、图鉴模块 `/dex`

### 13.1 `GET /dex`

```json
{
  "entries": [
    { "key": "lv5", "name": "初长成", "desc": "宠物达到 5 级",
      "target": 5, "progress": 5, "reward": 50, "unlocked": true, "claimed": false }
  ]
}
```

`progress` 已 clamp 到不超过 `target`。`unlocked = progress >= target`，`claimed` 是否已领奖。前端展示"可领取"红点的条件是 `unlocked && !claimed`。

**默认条目**：

| entryKey | 名称 | 达成条件 | 奖励 |
| --- | --- | --- | --- |
| `lv5` | 初长成 | 最高等级 5 | 50 |
| `lv15` | 风华正茂 | 最高等级 15 | 150 |
| `lv30` | 独当一面 | 最高等级 30 | 400 |
| `pet3` | 热闹家庭 | 拥有 3 只宠物 | 100 |
| `pet6` | 宠物大亨 | 拥有 6 只宠物 | 300 |
| `intimacy100` | 亲密无间 | 最高亲密度 100 | 120 |
| `skin3` | 衣橱初成 | 拥有 3 款皮肤 | 120 |
| `acc3` | 配饰收藏家 | 拥有 3 款配饰 | 120 |
| `furn4` | 安乐窝 | 拥有 4 件家具 | 200 |
| `collect10` | 博物学家 | 累计拥有 10 件物品 | 400 |

### 13.2 `POST /dex/claim`

请求 `{ "bizId": "...", "entryKey": "lv5" }`

```json
{ "entries": [ "...刷新后的完整列表" ], "gained": 50, "gameCoin": 1250 }
```

异常：400 `未知图鉴条目` / `图鉴条目尚未解锁` / `该图鉴奖励已领取`。

---

## 十四、扭蛋模块 `/gacha`

### 14.1 `GET /gacha`

```json
{
  "pools": [
    {
      "key": "daily", "name": "日常扭蛋", "pool": "game",
      "cost": 300, "costTen": 2700,
      "pity": 30, "dupeCoin": 120, "pityLeft": 25,
      "odds": [
        { "key": "coin_small",  "name": "零钱 60",       "rare": false, "percent": 42.0 },
        { "key": "coin_mid",    "name": "零钱 200",      "rare": false, "percent": 26.0 },
        { "key": "snack",       "name": "宠物零食 ×3",   "rare": false, "percent": 14.0 },
        { "key": "energy",      "name": "能量饮 ×2",     "rare": false, "percent": 9.0 },
        { "key": "cake",        "name": "生日蛋糕 ×1",   "rare": false, "percent": 6.0 },
        { "key": "bg_beach",    "name": "海边夕照（背景）", "rare": true, "percent": 2.0 },
        { "key": "skin_shadow", "name": "玄影（皮肤）",   "rare": true,  "percent": 1.0 }
      ]
    }
  ],
  "wallet": { "gameCoin": 3000, "marketingPoint": 0 }
}
```

> **`odds` 必须展示在界面上。** 这是虚拟道具抽取的合规要求。概率来自服务端实际使用的那份权重表，不存在"展示一套、跑另一套"的可能，直接渲染即可。

`pityLeft` 是距离保底还差几抽（`pity === 0` 表示该池不启用保底，此时 `pityLeft` 为 `null`）。

### 14.2 保底机制

- 每个玩家每个奖池独立维护"连续未出稀有"的计数。
- 累计到 `pity`（默认 30）抽时，**强制**从 `rare: true` 的档位中抽取。
- 抽到稀有档后计数归零。
- 十连中每一抽独立判定，第 3 抽触发保底后，后 7 抽从 0 重新累计。

### 14.3 重复收藏品折算

皮肤和配饰重复抽到时，会自动折算成游戏币（默认 120/件），响应里 `converted: true`。家具不折算（可以摆多份）。

### 14.4 `POST /gacha/draw`

请求 `{ "poolKey": "daily", "times": 10, "bizId": "..." }`

`times` **只能是 1 或 10**（中间档位既无定价也让保底判定复杂化）。

```json
{
  "poolKey": "daily",
  "times": 10,
  "cost": 2700,
  "prizes": [
    { "entryKey": "coin_small", "name": "零钱 60", "kind": "coin", "amount": 60, "itemKey": null, "qty": 0, "rare": false, "converted": false },
    { "entryKey": "skin_shadow", "name": "玄影（皮肤）", "kind": "item", "amount": 0, "itemKey": "skin_shadow", "qty": 1, "rare": true, "converted": false }
  ],
  "wallet": { "gameCoin": 360, "marketingPoint": 0 },
  "pity": 3,
  "duplicated": false
}
```

`prizes` 数组顺序即抽取顺序，前端按序播放开奖动画。`rare: true` 的档位应有特殊演出。

异常：400 `每次只能抽 1 次或 10 次` / `奖池不存在` / `余额不足`。

### 14.5 `GET /gacha/history`

Query：`page`、`pageSize`。返回 `{ list, total }`，每条记录含 `poolKey`、`times`、`cost`、`prizes`、`createdAt`。

---

## 十五、兑换中心 `/exchange` `/address`

### 15.1 `GET /exchange`

```json
{
  "items": [
    {
      "key": "plush_toy", "name": "宠物同款玩偶", "type": "physical",
      "cost": 2000, "pool": "marketing", "desc": "...", "sortOrder": 2,
      "stock": 100, "perUserLimit": 1,
      "grantItemKey": null, "grantQty": 0,
      "stockLeft": 87, "myLeft": 1
    }
  ],
  "wallet": { "gameCoin": 1200, "marketingPoint": 3000 }
}
```

`stockLeft` 和 `myLeft` 为 `null` 表示不限量/不限购。前端应在 `stockLeft === 0` 或 `myLeft === 0` 时禁用兑换按钮。

注意 `pool` 字段——**部分兑换项扣的是营销积分而不是游戏币**，UI 上要区分币种图标。

**默认兑换项**：

| exchangeKey | 名称 | 类型 | 花费 | 计费池 | 库存 | 限购 |
| --- | --- | --- | --- | --- | --- | --- |
| `coupon_5` | 5 元代金券 | virtual | 500 | marketing | 不限 | 不限 |
| `plush_toy` | 宠物同款玩偶 | physical | 2000 | marketing | 100 | 1 |
| `mug` | 定制马克杯 | physical | 1200 | marketing | 200 | 2 |
| `snack_pack` | 零食礼包 ×10 | virtual | 700 | game | 不限 | 不限 |
| `care_pack` | 护理礼包 ×5 | virtual | 900 | game | 不限 | 不限 |
| `energy_pack` | 能量礼包 ×5 | virtual | 1500 | game | 不限 | 不限 |

### 15.2 `POST /exchange/redeem`

请求 `{ "bizId": "...", "exchangeKey": "plush_toy", "addressId": "5" }`

**实物兑换（`type === "physical"`）必须传 `addressId`**，虚拟品不需要。

```json
{
  "order": { "...": "RedeemOrder" },
  "wallet": { "gameCoin": 1200, "marketingPoint": 1000 }
}
```

异常：400 `兑换项不存在` / `实物兑换需选择收货地址` / `该兑换项每人限兑 1 件，你已兑 1 件` / `该兑换项已兑完` / `余额不足`，404 `收货地址不存在`。

> 库存不足时服务端会**自动退款**再报错，玩家不会白扣钱。

### 15.3 订单状态

`GET /exchange/orders?page=1&pageSize=20` 返回 `{ list, total }`。

**`RedeemOrder` 字段**：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 订单 ID |
| `exchangeKey` | string | 兑换项 key |
| `itemName` | string | 商品名快照 |
| `itemType` | `physical` \| `virtual` | 类型 |
| `cost` | number | 花费 |
| `pool` | `game` \| `marketing` | 计费池 |
| `status` | `pending` \| `shipped` \| `cancelled` | 状态 |
| `address` | object \| null | 收货快照 `{ receiver, phone, region, detail }` |
| `trackingNo` | string \| null | 物流单号 |
| `shippedAt` | string \| null | 发货时间 |
| `cancelledAt` | string \| null | 取消时间 |
| `remark` | string \| null | 备注 |
| `createdAt` / `updatedAt` | string | ISO8601 |

**状态流转**：

```
pending ──运营发货──→ shipped（终态）
pending ──运营取消──→ cancelled（终态，自动退款 + 回滚库存/限购）
```

下单时的初始状态：

| 情况 | 初始 status |
| --- | --- |
| 实物 | `pending`（等运营发货） |
| 虚拟品且配了自动发放（如 `snack_pack`） | `shipped`，`remark = "自动发放"`，道具已入包 |
| 虚拟品但需人工处理（如代金券） | `pending` |

前端订单列表建议按 `status` 分三个 Tab：待处理 / 已完成 / 已取消。`trackingNo` 有值时展示物流单号并支持复制。

### 15.4 收货地址

| 接口 | 请求 | 响应 |
| --- | --- | --- |
| `GET /address` | — | `{ "list": UserAddress[] }` |
| `POST /address` | `{ receiver, phone, region, detail, isDefault? }` | `{ "address": UserAddress }` |
| `PUT /address/:id` | 同上所有字段可选 | `{ "address": UserAddress }` |
| `DELETE /address/:id` | — | `{ "ok": true }` |

`UserAddress`：`{ id, userId, receiver, phone, region, detail, isDefault, createdAt, updatedAt }`

字段长度限制：`receiver` ≤32、`phone` ≤20、`region` ≤128、`detail` ≤255。

列表按 `isDefault` 降序 + `id` 降序返回，所以**默认地址永远是第一条**。设置某个地址为默认时服务端会自动取消其他地址的默认标记。

异常：404 `地址不存在`。

---

## 十六、兑换码 `/promo`

### 16.1 `POST /promo/redeem`

请求 `{ "code": "ABCD-1234", "bizId": "..." }`

```json
{
  "code": "ABCD1234",
  "batch": "2026spring",
  "pool": "marketing",
  "amount": 500,
  "wallet": { "gameCoin": 1200, "marketingPoint": 3500 },
  "duplicated": false
}
```

**归一化在服务端做**——玩家输入的连字符、空格、大小写都会被处理掉，前端不需要自己清洗（但可以做输入体验优化）。提交长度上限 40 字符。

营销积分主要就靠这个入口发放。

**异常**：

| HTTP | message | 说明 |
| --- | --- | --- |
| 400 | `兑换码格式不正确` | 归一化后为空 |
| 404 | `兑换码无效或已失效` | 码不存在 |
| 400 | `兑换码无效或已失效` | 已停用或已过期 |
| 400 | `兑换码已被领完` | 达到最大可用次数 |
| 400 | `今日尝试次数过多，请明日再试` | 失败次数超限（默认 10 次/天，防爆破） |
| 400 | `今日兑换次数已达上限` | 成功次数超限（默认 5 次/天） |
| 400 | `兑换码正在处理中，请勿重复提交` | 并发提交同一个码 |

有防爆破限流，前端应该在连续失败几次后主动提示玩家检查输入，而不是让用户一直试到被限流。

### 16.2 `GET /promo/redemptions`

Query：`page`、`pageSize`。返回 `{ list, total }`，每条含 `code`、`pool`、`amount`、`createdAt`。

---

## 十七、错误码总表

### 17.1 通用错误

| HTTP | message | 触发场景 | 前端处理建议 |
| --- | --- | --- | --- |
| 400 | `["字段校验信息", ...]` | DTO 校验失败（含多传未知字段） | 开发期排查，线上兜底提示"参数错误" |
| 400 | `缺少幂等参数 bizId` | 幂等接口没传 bizId | 开发期 bug |
| 400 | `余额不足` | 任何扣费场景 | 引导充值/赚币 |
| 401 | `缺少访问令牌` | 没带 Authorization 头 | 重新登录 |
| 401 | `令牌无效或已过期` | JWT 失效 | **自动重新登录并重试原请求** |
| 403 | `账号已被封禁` / `账号已被封禁：{原因}` | 封禁账号发起写操作 | 弹封禁提示，展示原因，提供客服入口 |
| 409 | `请求处理中，请勿重复提交` | 幂等占位未释放 | 等 1–2 秒，**带同一 bizId** 重试 |
| 409 | `操作太频繁，请稍后再试` | 玩家级锁竞争 | 短暂延时后重试 |
| 500 | `服务器内部错误` | 未捕获异常 | 提示重试，上报日志 |

### 17.2 按模块的业务错误

| 模块 | HTTP | message |
| --- | --- | --- |
| 宠物 | 429 | `冷却中，还需 N 秒` |
| 宠物 | 400 | `体力不足` / `最多只能养 6 只宠物` / `暂无可领取的离线收益` |
| 宠物 | 404 | `宠物不存在` / `玩家不存在` |
| 每日 | 400 | `今日已签到` / `未知任务` / `该任务今日已领取` / `任务尚未完成` |
| 赛跑 | 400 | `未知赛道` / `体力不足，无法参赛` / `游戏币不足以支付报名门票` |
| 赛跑 | 400 | `请先结算本场比赛再翻倍` / `本场无奖励可翻倍` / `已结算的比赛不能复活重跑` / `本场复活机会已用完` |
| 赛跑 | 404 | `赛跑记录不存在` / `没有可参赛的宠物` |
| 广告 | 400 | `广告凭证无效或已过期` / `广告凭证与当前场景不匹配` |
| 广告 | 400 | `今日广告奖励次数已用尽` / `今日该场景的广告次数已用尽` |
| 换装 | 400 | `尚未拥有该物品` / `该物品不可穿戴` / `该物品无需购买` |
| 换装 | 404 | `物品不存在或已下架` / `没有可穿戴的宠物` |
| 消耗品 | 400 | `该消耗品数量不足` / `该消耗品暂未配置效果，请联系客服` |
| 消耗品 | 404 | `消耗品不存在` |
| 家园 | 400 | `该物品不是家具` / `可摆放数量不足，请先购买` / `该位置已有家具，请换个位置` |
| 家园 | 400 | `摆放位置超出房间范围（房间 6×6，该家具占 w×h）` / `房间已摆满，请先收纳部分家具` |
| 家园 | 404 | `摆放记录不存在` |
| 图鉴 | 400 | `未知图鉴条目` / `图鉴条目尚未解锁` / `该图鉴奖励已领取` |
| 扭蛋 | 400 | `每次只能抽 1 次或 10 次` / `奖池不存在` |
| 兑换 | 400 | `兑换项不存在` / `实物兑换需选择收货地址` / `该兑换项已兑完` / `该兑换项每人限兑 N 件，你已兑 M 件` |
| 兑换 | 404 | `收货地址不存在` / `地址不存在` |
| 兑换码 | 400 | `兑换码格式不正确` / `兑换码已被领完` / `今日兑换次数已达上限` / `今日尝试次数过多，请明日再试` |
| 兑换码 | 404 | `兑换码无效或已失效` |

所有 message 都是可以直接展示给玩家的中文，前端不需要维护一套错误码到文案的映射表。

---

## 十八、客户端封装参考

### 18.1 请求封装（自动补 token、401 重登、幂等重试）

```js
// game/net/request.js
import { getToken, login } from './auth';

const BASE_URL = 'https://your-api-domain.example.com';

function raw(path, { method = 'GET', data, query } = {}) {
  let url = BASE_URL + path;
  if (query) {
    const qs = Object.entries(query)
      .filter(([, v]) => v !== undefined && v !== null && v !== '')
      .map(([k, v]) => `${encodeURIComponent(k)}=${encodeURIComponent(v)}`)
      .join('&');
    if (qs) url += `?${qs}`;
  }

  return new Promise((resolve, reject) => {
    wx.request({
      url,
      method,
      data,
      header: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${getToken()}`,
      },
      success: (res) => {
        if (res.statusCode >= 200 && res.statusCode < 300) return resolve(res.data);
        const body = res.data || {};
        reject({
          code: res.statusCode,
          message: Array.isArray(body.message) ? body.message[0] : body.message,
        });
      },
      fail: (err) => reject({ code: -1, message: err.errMsg || '网络异常' }),
    });
  });
}

export async function request(path, options = {}) {
  try {
    return await raw(path, options);
  } catch (err) {
    // 令牌过期：重登一次后重放原请求（body 里的 bizId 不变，天然幂等）
    if (err.code === 401) {
      await login();
      return raw(path, options);
    }
    // 上一次还在处理：带同一 bizId 重试就能拿到结果
    if (err.code === 409) {
      await sleep(1200);
      return raw(path, options);
    }
    throw err;
  }
}

const sleep = (ms) => new Promise((r) => setTimeout(r, ms));
```

### 18.2 bizId 生成

```js
// game/net/bizid.js
let seq = 0;

export function newBizId(action) {
  seq += 1;
  const rand = Math.random().toString(36).slice(2, 8);
  return `${action}:${Date.now()}:${seq}:${rand}`;
}
```

要点：**一次用户操作生成一个 bizId 并持有它**，重试期间不要重新生成。

```js
// 正确：重试复用同一 bizId
const bizId = newBizId('feed');
await withRetry(() => request('/pet/feed', { method: 'POST', data: { bizId } }));

// 错误：每次重试都换新 ID，等于连喂三次
await withRetry(() => request('/pet/feed', { method: 'POST', data: { bizId: newBizId('feed') } }));
```

### 18.3 广告流程封装

```js
export async function watchAdAndReward(scene, consume) {
  // 1. 领凭证（不幂等，每次新的）
  const { nonce } = await request('/ad/token', { method: 'POST', data: { scene } });

  // 2. 播激励视频，必须在 300 秒内完成
  await playRewardedVideo();

  // 3. 核销 —— bizId 固定住，网络异常时重试不会重复发奖
  const bizId = newBizId(scene);
  return consume({ bizId, adToken: nonce });
}

// 用法
await watchAdAndReward('ad_reward', (payload) =>
  request('/ad/verify', { method: 'POST', data: payload }),
);

await watchAdAndReward('race_double', (payload) =>
  request('/race/reward/double', { method: 'POST', data: { ...payload, raceId } }),
);
```

---

## 十九、典型业务时序

### 19.1 冷启动

```
wx.login → POST /auth/login          拿 token
  ├─ GET /pet/state                  出战宠状态（没宠会自动建）
  ├─ GET /wallet                     余额
  ├─ GET /pet/offline                离线收益，有则弹领取弹窗
  └─ GET /daily                      签到红点 + 任务红点
```

后四个请求可以并发。首屏渲染只依赖 `/pet/state`，其余异步补齐。

### 19.2 一次完整的照顾

```
点击喂食
  → 本地检查冷却（避免无谓的 429）
  → POST /pet/feed { bizId }
  → 用响应的 pet 更新属性条，播 gained 飘字
  → capped 为 true 时提示"今日收益已达上限"
  → levelUp 为 true 时播升级动画
  → 按 cooldownRemainMs 起倒计时，按钮置灰
```

### 19.3 一场赛跑（含广告翻倍）

```
GET /race/tracks                     展示赛道、体力、门票
  → POST /race/start { bizId, trackKey }
  → 用 finishTime + opponentFinishTimes 播赛跑动画
  → 动画结束
     ├─ 名次不理想且 status=pending → 可选：走 race_revive 广告流程 → 重播动画
     └─ POST /race/settle { bizId, raceId }
        → duplicated 为 false 时播领奖动画
        → rewardCoin > 0 → 展示"看广告翻倍"按钮
           → 走 race_double 广告流程 → POST /race/reward/double
```

### 19.4 实物兑换

```
GET /exchange                        目录（注意区分 game / marketing 计费池）
  → 选中实物商品
  → GET /address                     没有地址则先引导 POST /address 新建
  → POST /exchange/redeem { bizId, exchangeKey, addressId }
  → 展示订单，status=pending，提示"等待发货"
  → 后续在订单页 GET /exchange/orders 查看 status 与 trackingNo
```

---

## 二十、联调 Checklist

前端接入完成后，建议逐项验证：

- [ ] `GET /health` 通，域名已配到微信 request 白名单
- [ ] 登录能拿到 token，token 持久化到 storage，冷启动复用
- [ ] 401 能自动重登并重放原请求
- [ ] 每个写操作都带了 `bizId`，且重试时复用而非重新生成
- [ ] 409 有重试逻辑，不会直接把"请求处理中"抛给玩家
- [ ] 没有给任何接口多传未定义字段（`forbidNonWhitelisted` 会直接 400）
- [ ] `petId` / `raceId` / `layoutId` / `addressId` 全程按 string 处理，没有 `Number()` 转换
- [ ] 扭蛋界面展示了 `odds` 概率公示（合规要求）
- [ ] 响应里的 `duplicated: true` 不会重复播放奖励动画
- [ ] 封禁账号（403）有明确的提示 UI，而不是静默失败
- [ ] 任务列表、赛道列表、商店商品都按接口返回渲染，没有硬编码 key
- [ ] 冷却按钮基于 `cooldownRemainMs` 本地倒计时，429 只作为兜底
- [ ] 离线收益为 0 时领取按钮禁用
- [ ] 兑换界面区分了游戏币和营销积分两种计费池的图标

---

## 附录：配置的可变性

文档里列出的所有**数值**（价格、奖励、冷却、赛道参数、任务目标、掉率、库存……）都是配置中心里的默认值，运营可以在后台随时调整，**调整后无需重启、即时生效**。

因此前端务必遵守：

- 数值一律从接口读，不要硬编码。
- 列表类数据（赛道、任务、商品、图鉴条目、奖池档位）按数组渲染，不要按固定 key 写死 UI。
- 美术资源用 `key` 做索引（`skin_snow.png`、`furn_sofa.png`），新增商品时只需补图。

运营新增一个赛道或一件商品时，理想情况下前端**不需要发版**。
